# FungiForge v0.2 — upgrade plan and progress record

**Living document.** This is the single source of truth for the v0.2 upgrade: what is being built, why, how each piece is validated, and where the work stands. It is updated every time an item changes state, and it is the first thing read when work resumes. Status board first, detail below.

Started 15 September 2026 from v0.1.0 (commit `92a484e`, tagged `v0.1.0`). Development happens on branch `develop`; `main` stays at v0.1.0 until the cluster's production runs (77-isolate ASSARM-PHI batch, C87 positive control) are complete, then v0.2.0 is merged, tagged and deployed with `bin/hpc_install.sh --ref v0.2.0 --rebuild-images`.

## Ground rules

1. **Every item ships four things**: the implementation, an automated test (unit test for Python, stub or nf-test for workflow logic), documentation (manual + README where user-facing), and a validation run on real data with recorded results. An item is not done until all four exist.
2. **No silent failure, anywhere.** Every stage JSON carries `status` (`ok | partial | failed | skipped`) and the exit status of each tool it ran. A tool failure is either fatal to the task or recorded as `failed` with the log tail; `|| true` is never used to make a stage look successful.
3. **Reproducible by construction.** Versioned image tags (no `latest`), exact conda pins, a per-run `provenance.json` listing every tool version, image digest and database release.
4. **Honest documentation.** The manual describes what the code does today. Aspirational text is labelled as roadmap.
5. **Validation data are fixed**: CEA10 hybrid (ONT SRR28036069 + Illumina SRR13127874), ASSARM-PHI-DF-005 Illumina-only (*A. flavus*), C87 hybrid TR34/L98H positive (ERR10820709 + ERR9791656), plus the controls added in W1.1. Expected results live in `test/expected/` and `bin/validate_run.py` compares a run against them with tolerances.
6. **Backwards compatibility of the master table**: columns are append-only; the Layer-2 scripts are updated in the same change as any new column.
7. Work in small commits on `develop`, each referencing the item ID (`W2.4:`), with the status board updated in the same commit.

## Status board

Legend: `todo` · `in progress` · `built` (code + tests) · `validated` (real-data run recorded) · `done` (validated + docs).

| ID | Item | Status | Commits | Validated on |
|---|---|---|---|---|
| W0.1 | Stage status contract, no silent failure | built | develop: `6af3383` | unit (25 tests), stub DAG, Docker fixture run; real-data run pending W0.4 |
| W0.2 | Pinned versions and full provenance | built | develop: `8deea53` | unit (48 tests total), stub DAG local + docker engine (provenance.json valid, 9/9 images resolved), two independent image builds identical (453 packages); real-data run pending W0.4 |
| W0.3 | Test framework and CI | built | develop: `8b25b82`, `a0a7d7d` | 127 unit tests (93 % coverage of bin/ + fungiforge/), 28 nf-test cases, lint 0 warnings, shellcheck clean, stub DAG, Docker fixture run; CI green on develop |
| W0.4 | Cluster development deployment (validate items on real data without touching production) | todo | | |
| W1.1 | Validation suite, controls, benchmarks | built | develop: `ea8c06b`, `6732256` | 142 unit tests incl. minimap2 integration; expected files for CEA10/DF-005; controls chosen (PRJNA1494181) — read-level verification, benchmark runs and control pipeline runs pending on the dev deployment |
| W2.1 | QC and verdict gating in the DAG | validated | develop: `c98a0b4` | stub DAG + nf-test (forced non_fungal, forced QC fail, --force_all), unit test for the master column; real case (DF-003, bacterial) pending on the dev deployment ; DF-003 (bacterial) stopped at the read-level gate on the dev deployment 2026-09-17 (7 min, 0.4 CPU-h, row with the reason) |
| W2.2 | Read QC: trimmed reads, k-mer profile, in-pipeline triage | built | develop: `919e722` | unit tests (GenomeScope/fastp/NanoPlot parsing, ploidy rule, triage), stub DAG + nf-test (read-level gate, task counts, off switches), Docker fixture run of the new stages; image rebuilt with kmc 3.2.4 + genomescope2 2.0.1 (lock refreshed); real-data validation pending on the dev deployment (DF-005 genome size vs assembly, CEA10 medaka model) |
| W2.3 | Identification: secondary loci, real concordance, species-aware BUSCO | built | develop: `f99c3eb` | 178 unit tests (marker extraction/chaining/strand, locus parsing with ties, concordance rule, MLST parsing, lineage map, download helpers with canned HTTP), stub DAG 55 tasks + nf-test (gate lists, stage 08/08b stubs), real-tool check in the image on the CEA10 assembly: CaM and BenA 100 % to *A. fumigatus* type material (NRRL 163 / CBS 133.61), LSU genus-level, MLST afumigatus ST5 (7/7 exact alleles), lineage eurotiales_odb10; real-data validation (DF-005 *A. flavus*/*A. oryzae* on CaM/BenA, CEA10 *A. fumigatus* + ST) pending on the dev deployment after `fetch_references.sh markers mlst busco` |
| W2.4 | Resistance: FungAMR-derived panel, read-level genotyping | built | develop: `e37bc38` | 196 unit tests (FungAMR derivation with tiers/synonyms/classes, panel merge, CDS codon coordinates on both strands with introns, per-read codon translation, zygosity, indel support, combined confidence, reads-only and discordant cases), nf-test stage 09 stub + DAG; in-image check on the real cyp51A locus with simulated reads: L98H confirmed from reads on the C87 assembly (96 %, TR34 agrees) and, on a wild-type assembly whose reads carry L98H + an extra TR unit, both reported as reads-only/discordant (the L26 case); real-data validation pending (controls PRJNA1494181 through the dev deployment, CEA10/DF-005 wild-type with read support) |
| W2.5 | Annotation: GeneMark, species-aware training, InterProScan, eggNOG | built | develop: `38373aa` | 201 unit tests (training map/choice with staged-resource checks, annotation statistics, report columns), nf-test (three new module stubs, DAG 61 tasks, InterProScan/eggNOG switches, gate lists), the two new images pulled and their entry points/versions verified; real-data validation pending on the dev deployment (CEA10/DF-005 gene counts and Pfam/GO/eggNOG coverage before vs after, GeneMark from the licensed directory) |
| W2.6 | Auxiliary biology: secretome, CAZymes, effectors, ploidy, mating type | built | develop: `a10b441` | 205 unit tests (dbCAN filter, SignalP/EffectorP/PHI-base parsers, MAT logic with GenBank synteny, nQuire parsing, CLI with and without tools), nf-test (stage 09 BAM contract, stage 13 stub, DAG), base image rebuilt with nQuire; real-tool check in that image on the 9,603 CEA10 proteins: 472 CAZyme proteins (dbCAN V14), EffectorP on a canned secretome, 1,841 PHI-base best hits, nQuire on a small BAM (haploid_like), 1.5 min; real-data validation pending on the dev deployment (CEA10 MAT1-1, DF-005 haploid, counts in published ranges; SignalP image built on site from the licensed package) |
| W2.7 | Organelle assembly and annotation | built | develop: `00e5882` | 210 unit tests (contig classification by genes/length/GC, HSP chaining with introns and copies, rRNA, circularity, pileup heteroplasmy, CLIs), nf-test (stage 04b stub, DAG 64), in-image check on the local CEA10 assembly (three overlapping Flye fragments recognised as redundant, size estimate 29.4 kb, 15/15 core genes, cox1 intron 2,011 bp as in Af293, rnl/rns 100 %, copy ratio 32 from a 50 k-read subsample); real-data validation pending on the dev deployment (CEA10 ~31 kb, 15/15 core genes, circular, copy ratio) |
| W2.8 | Mobile elements and mycovirus, made functional | built | develop: `ab47459` | 217 unit tests (RepeatMasker table on the real CEA10 summary, RVDB hit classification/loci, six-frame ORFs under genetic code 4 with a Pfam HEG hit, geNomad summaries, merge), nf-test (stage 06 table output, stage 10 inputs, DAG 64), in-image RVDB screen on the local CEA10 assembly (786 k-protein DIAMOND database in 4 s, 30 Mb blastx in 36 s, one partitivirus EVE candidate at 81 %, te_percent 4.67); real-data validation pending on the dev deployment (te_percent populated for both isolates, geNomad ok after fetch_references.sh genomad) |
| W2.9 | Novelty with a staged reference genome set | built | develop: `2068daf` | 221 unit tests (skani parsing with the aligned-fraction floor, the four verdicts incl. a synthetic 88 % genome flagged novel, ITS/secondary-locus rescue at 93.5 %, disagreement flag, manifest naming, fetcher with canned API responses), nf-test (stage 12 stub, DAG 64), in-image skani of the local CEA10 assembly against Af293 and NRRL 3357 fetched with the new script (0.4 s; ANI 99.59 % to Af293 at 94 % aligned fraction → known_species; the *A. flavus* genome falls under the aligned-fraction floor); real-data validation pending on the dev deployment after `fetch_references.sh genomes` |
| W3.1 | Phylogenomics and clonality | built | develop: `7134911` | 226 unit tests (BUSCO/compleasm single-copy layouts, skani edge parsing and ANI single-linkage clusters with the aligned-fraction guard, paftools call parsing, core intersection, SNP distances, clonal groups, trimming/concatenation/gene selection, CLI without tools), nf-test (stage 05 output, stage 16 stub, DAG 65, --skip_cohort), in-image mini cohort (CEA10 + Af293 + NRRL 3357: two ANI clusters, CEA10–Af293 99.59 % ANI and 42,457 core SNVs over a 27.4 Mb core = 1.55 per kb, no clonal group, 4.8 s; tree skipped below four isolates); real-data validation pending on the dev deployment (batch cohort: species clusters, tree, SNP distances; CEA10/C87/C6/E142 topology vs published) |
| W3.2 | Cohort BGC families and mycotoxin flags | built | develop: `a4b4a7d` | 231 unit tests (KnownClusterBlast JSON parsing under both key names, compound flags, region GenBank parsing, DIAMOND similarity and family clustering, CLIs, master columns), nf-test (stage 16 inputs, DAG 65), in-image family clustering of the 40 CEA10 antiSMASH regions duplicated as a second isolate (80 regions → exactly 40 families, each shared by both copies and none merged across distinct regions, 1.3 s); real-data validation pending on the dev deployment with `--cb-knownclusters` (DF-005 aflatoxin, CEA10 gliotoxin) |
| W3.3 | Reporting: per-isolate HTML, cohort MultiQC, merged master table, schema | built | develop: `c2195d1` | 238 unit tests (schema vs MASTER_COLS, type/enum/duplicate/missing-column validation, merge order, cohort tallies and metric ranges, MultiQC export, CLIs), nf-test (stage 17 stub, DAG 66), rendered on a synthetic 12-isolate cohort and a full CEA10-style isolate; real-data validation pending on the dev deployment (batch cohort report + Layer 2 end to end) |
| W4.1 | Engineering hygiene and documentation pass | built | develop: `65a4efe` | 245 unit tests (samplesheet validation: ids, pairing, missing/empty files, metadata typos, header problems, path resolution, CLI and strict mode), nf-test (purge stage stub, DAG 68), duplicate-id rejection verified on a real stub run; CHANGELOG, CITATION 0.2.0, manual sweep |
| W4.2 | Database fetch, installer and site config updates for new resources | built | develop: `37ebb4a` | the fetch steps landed with their items (markers, mlst, dbcan, phibase, effectorp, genomad, genomes, interproscan, multi-lineage busco, direct eggNOG); container-driven downloads now run under Apptainer/Singularity or Docker (L24), verified to degrade cleanly with no runtime; installer stages the SignalP image and pre-pulls the two new public images; deployment guide rewritten with the database table and the licensed resources |
| W5.1 | Cluster upgrade to v0.2.0 and re-validation | todo | | |
| W6.1 | Scheduled real-data regression on the cluster | todo | | v0.3 candidate |
| W6.2 | RNA-seq evidence path; funannotate 2 decision | todo | | v0.3 candidate |
| W6.3 | Resistance mechanisms beyond substitutions | todo | | v0.3 candidate |
| W6.4 | Chromosome-scale assessment: scaffolding, telomeres | todo | | v0.3 candidate |
| W6.5 | Typing resolution beyond seven loci | todo | | v0.3 candidate (size first) |
| W6.6 | RNA mycovirus detection | todo | | v0.3 candidate (needs W6.2) |
| W6.7 | External validation and publication | todo | | v0.3 candidate |
| W6.8 | Small specified items (report, InterProScan default, lock push) | todo | | v0.3 candidate |

## Limitations captured (inventory)

Everything the assessment and the code audit found, mapped to the item that closes it.

| # | Limitation | Evidence | Closed by |
|---|---|---|---|
| L1 | Blanket `\|\| true` on medaka, RepeatModeler/Masker, QUAST, BUSCO, funannotate predict/annotate, barrnap, ITSx, vsearch, sourmash, skani, geNomad, minimap2 | modules stage01–12 | W0.1 |
| L2 | Floating image tags (`latest` ×5), `>=` conda pins, provenance records six tools only — **resolved by W0.2** (versioned tags + digests, explicit conda lock, DB_CHECK, provenance.json with per-tool versions and image sha256) | base.config, env/*.yml, make_provenance.py | W0.2 |
| L3 | No unit tests, no CI; stub DAG tests plumbing only | repo | W0.3 |
| L4 | Resistance detection unproven on a positive; no read-level genotyping, no zygosity; panel has ~35 rows though FungAMR has thousands; species coverage is thin (no *A. flavus* cyp51C, *Cryptococcus*) | af_resistance_panel.tsv, af_resistance.py | W1.1, W2.4 |
| L5 | Non-fungal or QC-failed isolates still run every heavy stage | subworkflow | W2.1 |
| L6 | fastp output discarded; assembly uses untrimmed reads | stage01 | W2.2 |
| L7 | No genome-size, coverage, heterozygosity or ploidy estimate from reads | — | W2.2, W2.6 |
| L8 | Read-level triage is an external script, not a stage | bin/triage_reads.sh | W2.2 |
| L9 | "GCPSR multi-locus concordance" is ITS + ANI only; no TEF1α/RPB2/BenA/CaM, which ITS cannot resolve in *Aspergillus*, *Penicillium*, *Fusarium* — **built** in W2.3 (extract_markers.py + type-material sets + concordance rule) | id_classify.py | W2.3 |
| L10 | BUSCO `auto` lineage cannot be species-aware because QC runs before identification — **built** in W2.3 (stage 08b re-scores with the lineage chosen from the call) | subworkflow order | W2.3 |
| L11 | MLST claimed in stage 08 comments but not implemented — **built** in W2.3 (PubMLST fungal schemes, `mlst_fungal.sh`) | stage08 | W2.3 |
| L12 | GeneMark unlicensed; InterProScan only consumes a pre-computed XML; eggNOG not staged; no RNA-seq evidence path — **built** in W2.5 (GeneMark via `--genemark_dir` + key; eggNOG and InterProScan as stages 07b/07c in their own images; RNA-seq path stays future) | stage07 | W2.5 |
| L13 | Stage 13 is keyword tallies: secretome, CAZyme, virulence, mating type all effectively empty; ploidy `NA` — **built** in W2.6 (SignalP 6 / EffectorP 3 / dbCAN HMMs / PHI-base / Pfam MAT locus / nQuire) | extras.py | W2.6 |
| L14 | Mitochondrial split is an empty placeholder file — **built** in W2.7 (tblastn-based separation in stage 04, annotation in stage 04b) | stage04 | W2.7 |
| L15 | geNomad database is not in the fetch script, so stage 10 never ran; it targets the (empty) mito file; `te_percent` always NA; RVDB-prot is fetched but no stage reads it (found by W0.2's database audit) — **built** in W2.8 (fetch step `genomad`, whole-assembly run, RepeatMasker table → `te_percent`, RVDB mycovirus screen) | stage10, fetch_references.sh | W2.8 |
| L16 | `refseq_fungi_genomes` never staged, so genome-ANI novelty never ran; novelty is ITS-distance only — **built** in W2.9 (fetch step `genomes` from NCBI Datasets, skani against the set, ANI/ITS/secondary-locus rule) | stage12, fetch_references.sh | W2.9 |
| L17 | No cross-isolate phylogeny, SNP distances or clonality — **built** in W3.1 (run-level cohort stage: ANI clusters, BUSCO supermatrix tree, assembly-based core SNP distances and clonal groups) | — | W3.1 |
| L26 | Promoter tandem repeats (TR34/TR46) are called from the assembly only; a duplication absent from the assembly is invisible, and a genotype cannot be confirmed from reads. Found 2026-09-15: the reads deposited as C87 (ERR10820709 + ERR9791656) lack TR34 (0/84 ONT, 1/76 Illumina spanning reads) although the published C87 assembly GCA_949125185 carries it — the detector reports TR34 correctly on that assembly (now a fixture), so the accessions, not the detector, are wrong; a read-verifiable positive control is still needed — **built** in W2.4: the TR site is re-checked in the reads mapped to the assembly (extra copy = insertion, missing copy = deletion) | cyp51a_TR.py, af_resistance.py | W1.1, W2.4 |
| L27 | Medaka left ~0.3 % residual error at the C87 cyp51A locus (Polypolish changed 0.28–0.31 % of positions per contig): the medaka model is taken from `--dorado_model`, which does not match reads basecalled elsewhere (public data) | stage03 | W2.2 |
| L29 | Stage 05's optional QUAST call could never succeed (QUAST is in none of the images), so every run's assembly-QC status was `partial` and `stages_failed` never read `none`; the call is removed (statistics come from `assembly_qc.py`; the reference benchmark is `bin/benchmark_assembly.py`) | stage05 | W1.1 (done) |
| L30 | purge_dups (and minimap2) are not in the Flye image (`staphb/flye`), so the optional haplotig-purging step has exited 127 on every long-read isolate and the assembly stage has always been `partial` — **built** in W4.1 (stage 02c in the base image) | stage02 | W4.1 |
| L31 | NanoPlot (raw and filtered) exited 1 on the real CEA10 reads: NanoPlot 1.47.1 calls `plotly.figure_factory.create_2d_density`, removed in plotly 6 (the image has plotly 7; the conda package itself requires plotly ≥ 6.1), and static PNG export wants a Chrome-based Kaleido — **fixed** by running NanoPlot with `--plots dot --no_static` (statistics unaffected; verified in the image on synthetic ONT reads) | stage01 | W2.2 (done) |
| L28 | `polish.json` records `polypolish_bp_changed` as empty (the Polypolish log is not parsed); C87 produced no ITS at all (`its_present=false`), so identification fell back to sourmash at medium confidence — cause found 2026-09-16: barrnap fails with ENOSPC when the node's /tmp tmpfs is full (left full by crashed annotation tasks), and v0.1.x masked it with `\|\| true`; every task now gets its own work-dir /tmp (v0.1.3 / develop), and identify results of the production batch must be re-checked for silently missing ITS | stage03b, stage08 | W2.2, W2.3 |
| L18 | No BGC families across isolates, no mycotoxin cluster flags — **built** in W3.2 (KnownClusterBlast enabled, compound flags, cohort gene-cluster families) | bgc_summary.py | W3.2 |
| L19 | No per-isolate HTML report, no MultiQC, no merged `master_fungi.tsv` produced by the pipeline (Layer 2 gathers rows itself), no output schema — **built** in W3.3 (Jinja2 isolate report, stage 17 merged+validated master, cohort report, MultiQC custom content) | stage14, analysis/ | W3.3 |
| L20 | medaka is given the Dorado model name; a mismatch is hidden by `\|\| true` and falls back to the unpolished assembly silently | stage03 | W0.1, W2.2 |
| L21 | Samplesheet and metadata are not validated (compartment/facility/season free text) — **built** in W4.1 (`fungiforge check`, duplicate/malformed ids fail at launch, metadata typo warnings) | main.nf, cli.py | W4.1 |
| L22 | Manual describes mito, mycovirus and extras as working — **resolved** in W2.6/W2.7/W2.8 (the stages now do the work) and the manual rewritten section by section in W4.1 | docs/manual | W4.1 |
| L23 | Layer-2 R scripts are skeletons that assume columns not yet populated (`has_af_resistance`, `azole_R`, `echino_R`) — **built** in W3.3 (they derive from the populated columns, read the merged table and the cohort tables; the remaining objectives get their pass in W4.1) | analysis/scripts | W3.3, W4.1 |
| L24 | Fetch script downloads through Docker for antiSMASH/funannotate/eggNOG; needs Apptainer path — **built** in W4.2 (`crun`: Apptainer/Singularity first, Docker fallback; eggNOG is a direct download since W2.5) | fetch_references.sh | W4.2 |
| L25 | No chromosome-level scaffolding or telomere check for long-read assemblies; no RNA-seq path | — | out of scope for v0.2 (recorded) |

## Work items

### Phase 0 — Foundations

#### W0.1 Stage status contract
**Goal.** Make silent failure impossible. **Design.** A shared `bin/ff_status.py` helper writes `{status, tools: {name: {version, exit}}, note}` into every stage JSON; modules capture each tool's exit code explicitly; a tool that is optional (e.g. compleasm fallback to BUSCO) records `skipped` with the reason; anything else non-zero is `failed` and the task exits non-zero unless the stage is declared best-effort (BGC, extras), in which case `failed` is recorded and the run continues. `make_report.py` adds `stages_failed` (semicolon list) to the master table. **Validation.** Unit tests for the helper; a stub-run injection test that forces one tool to fail per stage and asserts the JSON status and the master column; CEA10 and DF-005 reruns must show all `ok`.

#### W0.2 Pinned versions and full provenance
**Goal.** Bit-for-bit reproducibility. **Design.** Replace every `latest` with a versioned tag and record the digest in `conf/base.config`; pin conda specs exactly (`==`) with a `conda-lock` file; `make_provenance.py` gathers all tool versions (from each container), image digests, database releases from `MANIFEST.tsv`, git commit, Nextflow version, and the effective parameters; a `--check-db` launch step verifies database completion markers and versions and fails early. **Validation.** Rebuilt images produce identical version tables on two hosts; provenance JSON validated against a schema in the test suite.

**As built (2026-09-15).** Images: `staphb/flye:2.9.6`, `staphb/spades:4.3.0`, `staphb/medaka:2.2.2`, `staphb/polypolish:0.6.1-bwa`, `ezlabgva/busco:v5.7.1_cv1`, `dfam/tetools:2.00` (the validated `latest`; `1.99` ships RepeatModeler 2.0.8/RepeatMasker 4.2.3, so it was rejected after checking), `nextgenusfs/funannotate@sha256:2eadfeb4…` (no versioned tag for the validated 1.8.17.dev197 build); each tag's manifest digest is recorded in `conf/base.config`, and `bin/fetch_references.sh images` pre-pulls exactly these. Conda: `env/base.yml` pinned `==` to the versions of the validated image; `env/base.linux-64.lock` is an explicit lock exported from a fresh build of those pins (the Mac's 0.1.0 image turned out to lack kraken2 — the lock check caught it — so the lock comes from a rebuild that includes it); `env/Dockerfile` and `env/fungiforge.def` install from the lock (`--build-arg ENV_SPEC=env/base.yml` bootstraps a new lock); `bin/lock_env.sh` regenerates and cross-checks. Provenance: `ff_version` in the status helper records each tool's version into the stage JSON (`tools[].version`, `versions`); `DB_CHECK` (stage 00, run-level, `bin/check_databases.py`) gates every isolate's first stage and fails the run with one list of missing databases unless `--allow_missing_db`; `PROVENANCE` (stage 15, run-level, `bin/make_provenance.py aggregate`) writes `pipeline_info/provenance.json` and validates it against `fungiforge/resources/provenance.schema.json`; `workflow.onComplete` adds image identities (sha256 of `.sif`/cached `.img`, or docker repo digests; `--provenance_hash_images`, `--image_cache_dir`). Basecall (stage 00) was also brought onto the status contract (it had been missed by W0.1). Validation done: 48 unit tests (`ff_version` end to end incl. missing/failing commands; DB checker; aggregate/images/validate; schema semantics); stub DAG 46/46 with `local` and with the `docker` engine (all nine images resolved with repo digests; document schema-valid); every version command exercised in the rebuilt image; two independent builds (from `base.yml` and from the lock) have identical 453-package sets. Still pending: real-data run on the dev deployment (W0.4), and the apptainer cache path on the cluster. Note for the record: tool-reported strings can differ from package versions (chopper 0.9.0 prints `0.8.0`, barrnap 1.10.6 prints `1.10.5`); the lock is authoritative.

#### W0.4 Cluster development deployment
**Goal.** Validate each item on real data while production keeps running. **Design.** A second, independent install at `/hpc/opt/fungiforge-dev` (`hpc_install.sh --prefix /hpc/opt/fungiforge-dev --ref develop --db /hpc/data/fungiforge --group <group>` with its own images and site config, no profile.d hook), run directories under `~/runs/dev/`. Items are validated there on CEA10 and DF-005 (and C87 once available) before being marked `validated`. **Validation.** The dev install runs the stub DAG and CEA10 end to end with the develop branch.

#### W0.3 Test framework and CI
**Goal.** Every later item ships with tests that run automatically. **Design.** `pytest` for all `bin/*.py` with fixtures under `test/fixtures/`; nf-test cases for the routing logic and each module's stub; GitHub Actions running unit tests, the stub DAG on the three-mode fixture, `nextflow lint`, and shellcheck; a nightly optional job running the CEA10 subsample through Docker. **Validation.** CI green on `develop`; coverage report for `bin/`.

**As built (2026-09-15).** Unit: 127 tests over every `bin/*.py` (13 new test files; synthetic GenBank/protein/promoter/Kraken2 fixtures, `test/fixtures/kraken2/`, shared `conftest.py`) and the CLI; scripts are driven as subprocesses with subprocess coverage (`.coveragerc`, `sitecustomize`), 93 % of `bin/` + `fungiforge/`, CI fails below 85 %. The tests found and fixed three real defects: `id_classify` never dropped `Genus_sp` hits (the `_sp$` check ran after underscores were replaced, so "Aspergillus sp" was reported as a species-level call), a panel row whose organism field contained "spp." applied to every genus in `af_resistance`, and a duplicated accession in one FungAMR cell double-counted its mutations; `make_report` no longer crashes on a `top_species` entry without `pct`. Static: `nextflow lint` at zero warnings — `Channel` → `channel`, explicit closure parameters, unused-parameter prefixes, and every task now calls the helpers from `bin/` on PATH (`source ff_status.sh`, `make_report.py …`) instead of `${projectDir}/bin/…` (verified inside the real containers by the Docker fixture run; all `bin/*.py` are executable); shellcheck clean at warning level on every shell script (directives only where the dynamic `source` is intended). nf-test 0.9.5 (`nf-test.config`, `test/nf-test/`): six routing cases on `parse_row`/`resolve_path`, one stub test per module (19), and three whole-DAG cases (46 tasks; skip flags remove 12; `--help` launches nothing) — 28 cases. First CI run failed on the two Nextflow jobs because the per-label memory requests (16 GB) exceed a hosted runner; `process.resourceLimits` in `conf/test.config` now clamps the machinery check, and run 34944666364 is green on all five jobs. `test/run_docker_fixture.sh` runs the fixture through the real images: DB_CHECK and READ_QC complete in-container with versions recorded, and Flye/SPAdes fail on the synthetic reads with the failure recorded by the contract (the expected outcome; it is the in-container check the hosted CI cannot do). CI (`.github/workflows/ci.yml`): unit (Python 3.11/3.12, coverage artifact), lint, stub DAG (validates `provenance.json`), nf-test. **Deviation from the design:** the nightly CEA10 Docker job is not feasible on a hosted runner (~40 GB databases, ~30 GB images); real-data validation stays on the development deployment (W0.4/W1.1) and the Docker fixture run is documented as a local check.

### Phase 1 — Validation

#### W1.1 Validation suite, controls and benchmarks
**Goal.** Demonstrated, not designed. **Design.** Controls: C87 (TR34/L98H), a *C. auris* FKS1 S639F isolate and a *C. albicans* ERG11 isolate from the SRA (accessions recorded here when chosen), the wild-type siblings C6 and E142; reference benchmarks: CEA10 against its published assembly and DF-005 against *A. flavus* NRRL 3357 using QUAST (misassemblies, NGA50, indels per 100 kb) and BUSCO; `test/expected/<sample>.json` with tolerances; `bin/validate_run.py` producing a pass/fail table. **Validation.** All controls call their known genotype at high confidence; benchmarks within tolerance; results recorded in `docs/validation.md`.

**As built (2026-09-16).** `bin/validate_run.py` (also `fungiforge validate`) compares a run with `test/expected/<sample>.json` — rules `equals`, `one_of`, `regex`, `min`, `max`, `expected` ± `abs_tol`/`rel_tol`, values read from stage JSONs, the master row or provenance — and prints a pass/fail table (exit 1 on failure); expected files for CEA10 (hybrid) and DF-005 (Illumina-only) written from the recorded v0.1.0 values with explicit tolerances. `bin/benchmark_assembly.py` gives the QUAST-style numbers (aligned fractions, NGA50, breakpoints, mismatches and indels per 100 kb, unaligned contigs) from minimap2 + the cs tag, because QUAST is in none of the images; `fetch_references.sh benchmarks` stages A1163 and NRRL 3357. Controls: the C87 accessions were shown to be a different isolate from the published assembly (see docs/validation.md §3.1), so controls are now taken from PRJNA1494181 (ONT, Dorado hac): two TR34/L98H isolates and a matched wild-type sharing the same cyp51A background; `bin/control_genotype.py` verifies a control's genotype from its reads at a wild-type locus (`test/controls/afum_cyp51A_wt_locus.fa`, derived from the published C87 assembly with one TR unit removed and L98H reverted — identical to AF338659 over its 352-bp promoter and exon structure) with insertion-window and point-mutation checks, and `bin/fetch_controls.sh` downloads, verifies and writes a samplesheet row per control, refusing any whose reads fail the check. Tests: 15 new (rules, table/exit status, SAM walking, call thresholds, spec-locus consistency, minimap2 integration on simulated ONT reads for TR34/L98H and wild-type, benchmark metrics on hand-built and synthetic alignments); minimap2 added to the CI unit job. Also fixed: the phantom QUAST step (L29). *Candida* controls deferred to W2.4. **Pending for `validated`:** the dev-deployment CEA10/DF-005 runs compared with the expected files, the two reference benchmarks, and the three poultry-farm controls fetched, read-verified and run.

### Phase 2 — Correctness of existing stages

#### W2.1 QC and verdict gating in the DAG
**Design.** After stage 04 and 05, isolates with `verdict in (non_fungal, human)` or `qc_pass == false` are routed to a `SKIPPED_REPORT` path that emits the master row with `stages_failed` and the reason, skipping stages 06–13; `--force_all` overrides. **Validation.** Stub test with a forced non-fungal verdict; DF-003 (bacterial) as the real case: row present, no heavy stage executed.

**As built (2026-09-16).** The subworkflow reads each isolate's decontam and assembly-QC JSONs after Stage 05 and stops isolates with verdict `non_fungal`/`human` or `qc_pass == false` (`mixed` and `not_run` pass); a small `GATE` process (`modules/stage05b_gate.nf`) writes `<id>.gate.json` (status `skipped`, reason, the eight stages not run) so REPORT still produces the master row — new append-only column `gate` (`pass` | `skipped(<reason>)`) and `gate:skipped` in `stages_failed`; stages 06–13 receive only isolates that passed (`nuclear_ok` channel), everything downstream inherits the filter through its joins. `--force_all` disables the gate. Test hooks for stub runs: `--stub_verdicts 'ID=non_fungal'` and `--stub_qc_fail 'ID'` make the decontam/QC stubs emit the forcing values; nf-test cases assert the task count (46 − 2×8 + 2), that no fungal stage ran for the stopped isolates, the gate JSONs and the provenance entries, and that `--force_all` restores 46 tasks. Folded in: Medaka now resolves its model from the reads' `basecall_model_version_id` header (Dorado) and otherwise uses `--medaka_model` (default: the `--dorado_model` string) after checking it against `medaka tools list_models` — an unknown model fails the stage instead of polishing with the wrong one (L27); the Polypolish change count is summed from its per-contig log lines into `polish.json` (L28, first half). **Pending for `validated`:** DF-003 (bacterial) through the dev deployment: row present with `gate = skipped(verdict:non_fungal)`, no annotation task; a fungal isolate unchanged.

#### W2.2 Read QC upgrade
**Design.** Route fastp-trimmed reads to assembly and polishing; add k-mer profiling (KMC3 + GenomeScope2) giving genome size, heterozygosity, ploidy hint and expected coverage into `readqc.json`; add an optional in-pipeline read triage (Kraken2 on a subsample) as stage 00b writing the verdict early so W2.1 can gate before assembly; correct medaka model mapping (derive from the Dorado model string, validate against `medaka tools list_models`, fail if unknown). **Validation.** Unit tests for the model mapping and GenomeScope parsing; DF-005 genome size within 10 % of assembly length; CEA10 medaka runs with the correct model (log inspected).

**As built (2026-09-17).** READ_QC now emits the fastp-trimmed Illumina pairs (`<id>_R1/2.trim.fastq.gz`) and everything downstream (SPAdes, Polypolish, triage, k-mer profile, extras) consumes them; `bin/read_stats.py` folds fastp's JSON and NanoPlot's NanoStats (raw and filtered) into `readqc.json` (`read_stats`, `total_bases`, `ont_n50`). New stage 00b `READ_TRIAGE` (`modules/stage00b_triage.nf`, `bin/read_triage.py`): seqkit subsample (`--triage_reads`, default 200k; Illumina when present, else ONT) → Kraken2 → the shared verdict rules (`kraken_taxonomy.py`) → `<id>.triage.json`; the subworkflow stops `non_fungal`/`human` isolates before assembly through the same GATE process (reason `read_triage:<verdict>`, 14 skipped stages), `--read_triage false` / `--force_all` as switches, `not_run` without a Kraken2 DB. New stage 01b `KMER_PROFILE` (`modules/stage01b_kmer.nf`, `bin/kmer_profile.py`): KMC 3.2.4 (`-ci1 -cs100000`, k = `--kmer_k` 21) → histogram → GenomeScope2 2.0.1 at ploidy 1 and 2 → genome size, heterozygosity, ploidy hint (diploid only when the p=2 fit is at least as good and heterozygosity ≥ 0.5 %), k-mer coverage and read coverage (bases / genome size); tools are optional steps (partial + nulls with a note on non-convergence). Master table gains `read_verdict`, `genome_size_est`, `heterozygosity_pct`, `ploidy_hint`, `coverage` (append-only). Base image: `kmc==3.2.4`, `genomescope2==2.0.1` (R 4.4) added to `env/base.yml`, image rebuilt from the yml and the explicit lock re-exported (471 packages). Tests: 10 new unit tests (GenomeScope summary/model parsing, ploidy rule, histogram totals, CLI with and without fits, fastp/NanoPlot parsing and merge, triage verdicts), nf-test DAG cases (52 tasks; read-level gate 52−17+4; off switches give the W2.1 DAG of 46), module stubs for both stages, stub hook `--stub_read_verdicts`. Real-tool check inside the rebuilt image: a simulated 500 kb haploid genome with 50× paired reads (0.5 % error) gave GenomeScope genome size 495.7 kb (−0.9 %), ploidy hint haploid, coverage 46 (k-mer) / 50 (read bases). The Medaka model mapping half of this item was delivered with W2.1. **Pending for `validated`:** dev-deployment runs with the rebuilt image — DF-005 `genome_size_est` within 10 % of its assembly length, CEA10's medaka model resolved from the read headers (log), a bacterial study isolate (DF-003) stopped at read triage.

#### W2.3 Identification upgrade
**Design.** Extract secondary loci from the assembly with curated reference sets and HMMs: LSU (D1/D2), TEF1α, RPB2, β-tubulin (BenA), calmodulin (CaM); search each against a curated marker database (UNITE for ITS; per-locus references from RefSeq/GenBank type material, staged by W4.2); true concordance: species call requires ITS and at least one secondary locus to agree, else genus-level with a flag; run BUSCO twice: `fungi_odb10` for the early gate, then the class/order lineage chosen from the species call for the reported completeness; implement MLST via `mlst` for schemes that exist (*A. fumigatus*, *C. albicans*, *C. glabrata*, *C. tropicalis*, *C. krusei*). **Validation.** DF-005 resolves *A. flavus* vs *A. oryzae* on CaM/BenA; CEA10 stays *A. fumigatus*; unit tests on marker extraction.

**As built (2026-09-17).** Stage 08 now extracts the secondary loci from the assembly: `bin/extract_markers.py` locates CaM, BenA, TEF1 and RPB2 by tblastn of bundled reference proteins (`fungiforge/resources/markers/marker_reference_proteins.faa`: Af293 calmodulin XP_751821.2, β-tubulin XP_752456.1, TEF1 XP_750388.2, *S. cerevisiae* RPB2 P08518), chains the HSPs of the best contig/strand across introns (`--max-gap`), and writes each locus ± flank in coding orientation; LSU D1/D2 is the first 900 bp of barrnap's longest 28S. Each locus is then searched (megablast) against a per-locus reference set of NCBI records flagged *sequence from type* (`bin/fetch_marker_refs.py`, staged by `fetch_references.sh markers` into `<data_dir>/markers/<locus>.fasta`; BLAST databases are built inside the task). `bin/id_classify.py` applies the concordance rule: **high** = ITS species + at least one agreeing secondary line (locus or sourmash genome match) and no contradicting one; **medium** = ITS alone (flag `secondary_loci_unavailable` / `no_secondary_species_call`), secondary-only, ITS 94–98.5 %, or ties that include the ITS species; **low** = genus only, or discordance (reported as *Genus* sp. with `discordant:<locus>=<species>`). Reference species within 0.3 % identity of the best hit make a locus a *tie* that never confirms a species by itself but lists the candidates (the *A. flavus* / *A. oryzae* case). MLST (L11): `bin/fetch_mlst_schemes.py` stages the PubMLST *A. fumigatus*, *C. albicans*, *C. glabrata*, *C. tropicalis* and *C. krusei* schemes (`fetch_references.sh mlst`), `bin/mlst_fungal.sh` builds the BLAST database in the task and runs `mlst --datadir/--blastdb`; scheme, ST and alleles go into `identify.json` and `mlst_st` on the master row, with a flag when the scheme's genus disagrees with the call. Species-aware BUSCO (L10): stage 08 chooses the lineage from `fungiforge/resources/busco_lineages.tsv` (73 genera → odb10 order/class lineages, species rows possible), new stage 08b `BUSCO_LINEAGE` re-scores the nuclear assembly with it (skipped with a reason when it equals the stage-05 lineage, is not staged, or `--busco_lineage` is fixed); `fetch_references.sh busco` now stages the common lineages by direct download from the BUSCO data server into `<data_dir>/busco/lineages/` (no container needed; `BUSCO_LINEAGES` overrides the set; remove `<data_dir>/busco/.done` to add lineages to an existing root). Master columns appended: `id_loci_agree`, `id_flags`, `mlst_st`, `busco_lineage_specific`, `busco_complete_specific`. LSU D1/D2 is genus-level evidence only (never a species call): on CEA10 it is 99.9 % identical to eight section *Fumigati* type strains. Every new database is optional (`DB_CHECK` reports presence; absent → the step records `skipped`, the call falls back to ITS-only medium). Gate lists include `busco_lineage`; stub DAG is 55 tasks. Real-tool check in the base image on the local CEA10 assembly with freshly fetched sets (CaM 3,829, BenA 8,031, LSU 55,721 type-material records; afumigatus + calbicans schemes): all five loci extracted (CaM 2 HSPs across an intron, BenA 3, TEF1 1, RPB2 1 on the minus strand, 28S 3.4 kb), CaM and BenA 100 % to *A. fumigatus* NRRL 163 / CBS 133.61 partial cds, LSU genus *Aspergillus*, MLST afumigatus ST5 with 7/7 exact alleles, secondary-only call *A. fumigatus* (medium without ITS in this check), lineage eurotiales_odb10; found on the way: BLAST `stitle` starts with the accession (parser fixed), unverified records excluded from the fetch queries, a Python 3.12-only f-string that the 3.11 image rejected. **Pending for `validated`:** dev-deployment runs after `fetch_references.sh markers mlst busco`: DF-005 (expect ITS *A. flavus* with CaM/BenA ties listing *A. flavus*/*A. oryzae*, TEF1/RPB2 to decide), CEA10 (expect high with ≥ 1 agreeing locus, afumigatus ST, eurotiales_odb10 completeness).

#### W2.4 Resistance upgrade
**Design.** Generate the panel from FungAMR programmatically (`build_fungamr_refs.py` extended) with species, gene, position, wild-type, mutant, drug class, evidence tier; keep the assembly-level path; add read-level genotyping: map reads (minimap2 for ONT, bwa-mem2 for Illumina) to the species reference gene set, call variants with bcftools, translate to protein changes, report allele frequency and zygosity; confidence combines polish mode, read support and evidence tier; species-specific extras: *A. fumigatus* cyp51A TR (kept), *A. flavus* cyp51C hotspots, *Candida* ERG11/FKS1/FKS2 hotspot regions, *Cryptococcus* FCY1/FUR1/ERG11; copy-number signal for ERG11/CDR1 from coverage. **Validation.** Controls in W1.1 call their genotypes; CEA10/DF-005 remain wild-type; unit tests on panel generation and variant translation.

**As built (2026-09-17).** *Panel.* `bin/fungamr_panel.py` derives panel rows from the FungAMR table (one per species × gene; every simple substitution with its strongest positive confidence score as `evidence_tier`, per-mutation drug classes, species synonyms such as *Candidozyma auris* ↔ *Candida auris*, negative and unscored rows dropped): 330 species/gene rows and 5,347 mutation entries from FungAMR 070425. `af_resistance.py` merges these into the curated overlay at run time (`<data_dir>/fungamr/fungamr_panel.tsv` when `build_fungamr_refs.py` wrote it, else derived on the fly from the raw table in under a second, so existing data roots need no new step; `--no-fungamr-panel` for the overlay alone). Tier-8-only changes (seen in a resistant isolate without validation) are reported as `associated_unvalidated`, never as resistance; overlay mutations are `curated`. *Read-level genotyping.* Stage 09 now receives the QC'd reads, maps a subsample (`--genotype_reads` 3 M Illumina pairs / `--genotype_reads_ont` 200 k ONT reads; minimap2 `sr` / `map-ont`) to the GenBank records (`bin/gbk_to_fasta.py`: the annotation's coordinate system, not the assembly's) and `bin/read_genotype.py` translates every read at each hotspot codon (CDS coordinates from the GenBank, both strands, introns) → allele counts, frequency, zygosity (`homozygous` ≥ 80 %, `heterozygous`, `mixed`, `insufficient` < 10 reads). Each assembly call gains `read_support` and `agrees`; known alleles present in the reads at ≥ 20 % but absent from the assembly are reported as reads-only variants (heterozygous in a diploid, or an assembly error); the cyp51A TR site is checked as an insertion/deletion in spanning reads against the assembly (`agrees_with_assembly` / `reads_have_extra_copy` / `reads_lack_copy`), so a duplication the assembly missed (L26) becomes a TR call with `discordant` confidence and a note; locus depth over length-weighted genome depth gives `copy_number` per target/efflux gene (`possible_duplication` ≥ 1.8). *Confidence.* `high` = read-confirmed or a hybrid/Illumina assembly; `provisional_ont_only` = ONT assembly without read confirmation; `discordant` = reads contradict the assembly; `low_evidence` = tier 8; `provisional_minor_allele` = reads-only below 80 %. Master columns appended: `resistance_read_support` (`confirmed:N;discordant:M;reads_only:K;insufficient:J`), `copy_number_flags`. `--read_genotype false` restores the assembly-only path. Species-specific extras came in through the derived rows (*A. flavus* cyp51A/B/C, *Candida* ERG11/FKS1/FKS2, *Cryptococcus*, *Pneumocystis* DHFR …) — the curated overlay keeps the promoter TR, LoF and GOF mechanisms the catalogue does not encode as substitutions. Real-tool check in the base image on the real cyp51A locus (test/controls wild-type locus and the published C87 slice) with simulated 250-bp reads: C87 assembly → L98H confirmed by 25 reads (96 % H), TR34 `agrees_with_assembly`, copy ratio 1.1; wild-type assembly with reads carrying L98H and an extra TR unit → L98H reads-only (97 %), TR34 `reads_have_extra_copy` (12/12 spanning reads) reported as discordant. Found on the way: FungAMR tokens whose mutant letter equals the reference residue (numbering drift) produced synonymous "reads-only" calls — wild-type alleles are now skipped. **Pending for `validated`:** the three PRJNA1494181 controls (two TR34/L98H, one matched wild-type) through the dev deployment with read support recorded in docs/validation.md; CEA10/DF-005 stay wild-type with `resistance_read_support` showing confirmed positions.

#### W2.5 Annotation upgrade
**Design.** GeneMark-ES via `--genemark_key` (key installation documented, Appendix A); species-aware `--busco_db` and Augustus species from the stage-08 call; InterProScan as its own containerised stage (07b) with the InterPro data staged by W4.2, feeding `funannotate annotate --iprscan`; eggNOG-mapper database staged; BRAKER3 offered as `--annotator braker3` when RNA-seq is provided (future). **Validation.** DF-005 and CEA10 gene counts and BUSCO-protein completeness compared before and after; Pfam/GO coverage reported.

**As built (2026-09-17).** Stage 07 is now four processes. *07a `PREDICT`* (`modules/stage07_predict.nf`): `funannotate predict` with GeneMark-ES when `--genemark_dir` (the unpacked licensed `gmes_linux_64` directory, bound into the container by every profile) and `--genemark_key` are given — the funannotate image does **not** ship GeneMark (Appendix A corrected); the key is copied to the task's `$HOME/.gm_key`, `GENEMARK_PATH` exported, and `predict.json` records `genemark: yes|no`. Training is species-aware: `bin/annotation_training.py` maps the stage-08 species call through `fungiforge/resources/annotation_training.tsv` (85 taxa: Augustus pre-trained seed species from `funannotate species` and the funannotate BUSCO set — eurotiomycetes / saccharomycetes / sordariomycetes / basidiomycota / ascomycota / fungi) and uses a mapped choice only when it is actually staged (Augustus `species/<name>` directory, `$FUNANNOTATE_DB/<set>`), else anidulans / dikarya with the reason in the JSON. IDENTIFY therefore runs before prediction (it never depended on annotation). *07b `EGGNOG`* (`quay.io/biocontainers/eggnog-mapper:2.1.15`, digest in `conf/base.config`): emapper on the predicted proteins with the eggNOG 5.0.2 data (`fetch_references.sh eggnog` now downloads `eggnog.db`, `eggnog_proteins.dmnd` and the taxa file directly, no container; ~12 GB compressed), fungal `--tax_scope 4751`; skipped inside the stage when the data are absent, `--skip_eggnog` removes it. *07c `INTERPROSCAN`* (`interpro/interproscan:5.78-109.0`, data release 5.78-109.0 staged by `fetch_references.sh interproscan`, 6.9 GB, bound at `/opt/interproscan/data` via `--interproscan_data`): XML with GO terms and pathways; `--run_interproscan true` adds it (off by default: hours per genome). *07 `ANNOTATE`*: `funannotate annotate` on a private copy of `predict_results` with `--eggnog` / `--iprscan` when the tables are non-empty, then `bin/annotate_stats.py` reads funannotate's annotations table: proteins, % with PFAM / InterPro / GO / eggNOG / a named product, secreted, CAZyme, protease, BUSCO and EC counts into `annotate.json`. Master columns appended: `n_proteins`, `pct_pfam`, `pct_go`, `pct_eggnog`, `pct_interpro`, `annotation_training` (`<augustus>/<busco_db>/genemark:<yes|no>`). Gate lists include predict/eggnog/interproscan; stub DAG is 61 tasks (64 with InterProScan). BRAKER3 stays future. Verified in the images: emapper 2.1.15 expects DB 5.0.2 (the staged version), InterProScan 5.78-109.0 keeps `interproscan.sh` off PATH behind an ENTRYPOINT (called by path; docker profile clears the entrypoint like antiSMASH). **Pending for `validated`:** on the dev deployment with `--genemark_dir /hpc/opt/licensed/genemark/gmes_linux_64_4 --genemark_key /hpc/opt/licensed/genemark/gm_key` after `fetch_references.sh eggnog` (and optionally `interproscan`): CEA10 and DF-005 gene counts and BUSCO-protein completeness before vs after, Pfam/GO/eggNOG coverage from the new columns.

#### W2.6 Auxiliary biology
**Design.** SignalP 6 (academic licence, Appendix A) and EffectorP 3 for secretome and effectors; dbCAN3 (`run_dbcan`) for CAZymes; nQuire and Smudgeplot on the reads for ploidy and heterozygosity; MAT locus typing by HMM (Pfam MATalpha_HMGbox PF04769, HMG box PF00505 in the MAT1-2-1 context) plus synteny check. **Validation.** *A. fumigatus* CEA10 is MAT1-1 (published); DF-005 haploid; counts non-zero and within published ranges.

**As built (2026-09-17).** `bin/extras.py` replaced the keyword tallies with tool-backed blocks, each optional (missing tool or database → `null` with a reason in `tools`/`skipped`, stage `partial`, never failed). *CAZymes:* `hmmsearch` of the proteins against the dbCAN family HMMs (`fetch_references.sh dbcan`, dbCAN-HMMdb V13) with run_dbcan's HMMER filter (E ≤ 1e-15, HMM coverage ≥ 0.35): families per protein, counts by class. *Secretome and effectors:* SignalP 6 (`signalp6 … --organism eukarya --mode fast`) when present — it is licensed, so `env/signalp6.Dockerfile` layers the DTU package and CPU torch onto the base image and `bin/hpc_install.sh --signalp <tarball>` builds it on site into `images/fungiforge-signalp6-<ver>.sif` and points the `extras` label at it in site.config (never pushed to a registry); EffectorP 3 (`fetch_references.sh effectorp`: the GPL repository with its bundled WEKA, run with the Java already in the base image) on the signal-peptide proteins → cytoplasmic/apoplastic effectors. *Virulence:* diamond blastp against PHI-base (`fetch_references.sh phibase`, CC BY FASTA), best hit per protein with identity ≥ 40 % and coverage ≥ 50 %, counts by PHI-base phenotype. *Mating type:* Pfam profiles fetched from the funannotate database's Pfam-A.hmm (MATalpha_HMGbox PF04769, HMG_box PF00505, Exo_endo_phos PF03372 = APN2, I_LWEQ PF01608 = SLA2, Homeodomain PF00046): an alpha-box protein = MAT1-1 (MTLα1); an HMG-box protein within six genes of APN2/SLA2 in the GenBank = MAT1-2-1 (fallback: a single-domain HMG-box protein, flagged as lacking synteny evidence); both = homothallic or heterozygous diploid MTLa/α. *Ploidy:* nQuire (built from source in a Dockerfile builder stage; not on bioconda) on the stage-09 read BAM (`create -q 20 -c 10` → `denoise` → `lrdmodel`): best of the diploid/triploid/tetraploid models, or `haploid_like` when fewer than 1,000 biallelic sites survive denoising; GenomeScope's k-mer hint (W2.2) remains the second opinion. Smudgeplot was not added: its current release depends on FastK, which is not packaged. Real-tool check in the rebuilt image on the CEA10 proteins and GenBank with freshly fetched data (dbCAN V14: 875 family HMMs; PHI-base 4.19: 10,023 sequences; EffectorP 3.0 at 157a99d): 472 CAZyme proteins (GH 1016 family assignments, GT 97, AA 113, CE 32, PL 68, CBM 28 — the published *A. fumigatus* CAZyme complement is about 500), EffectorP ran on a canned 40-protein secretome (18 cytoplasmic effectors), 1,841 proteins with a PHI-base best hit, nQuire on a small BAM reported `haploid_like` (0 biallelic sites); 1.5 min for the whole stage. Found on the way: the dbCAN site now serves its files only through a download script (plain paths return HTML; the fetch step checks for the HMMER3 header), and PHI-base headers carry six `#` fields. Stage 13 now takes the proteins, the GenBank and the stage-09 BAM (RESISTANCE emits `<id>.reads.bam` + index, an empty placeholder when mapping did not run). Master columns appended: `n_secreted`, `n_effectors`, `n_cazymes`, `n_phibase_hits`; `mating_type` and `ploidy` are now HMM- and read-based. **Pending for `validated`:** dev-deployment runs with the rebuilt image and `fetch_references.sh dbcan phibase effectorp` (plus `hpc_install.sh --signalp /hpc/opt/licensed/signalp6/signalp-6.0i.fast.tar.gz --rebuild-images`): CEA10 MAT1-1 (published), DF-005 haploid, secretome/CAZyme/effector counts within the ranges published for *A. fumigatus* and *A. flavus*.

#### W2.7 Organelle assembly
**Design.** GetOrganelle (`fungus_mt`) from reads, oatk as alternative for ONT; MITOS2 or MFannot annotation; mito size, gene set, introns, and homoplasmy; feeds stage 10. **Validation.** CEA10 mitogenome ~32 kb with the expected gene set.

**As built (2026-09-17).** Assembly-first rather than read-first: both Flye and SPAdes assemble the mitogenome, so stage 04 now *separates* it — `bin/mito_extract.py` calls a contig mitochondrial when tblastn of the bundled core proteins (`fungiforge/resources/mito/mito_core_proteins.faa`: cox1-3, cob, atp6/8/9, nad1-6, nad4L, rps3 from *A. fumigatus* Af293, *S. cerevisiae* and *C. neoformans*) finds at least two distinct core genes on it, it is at most `--mito_max_len` (250 kb) and AT-rich (GC < 40 %, which keeps a NUMT-bearing chromosome nuclear); the contigs leave `nuclear.fasta` (L14 closed: stage 10 and the annotation now see a real split) and `decontam.json` lists them with the rejected candidates and their reasons. New stage 04b `ORGANELLE` (`modules/stage04b_organelle.nf`, `bin/mito_annotate.py`): per core gene the best species' HSPs on the best contig/strand are chained (gaps ≥ 100 bp = introns) → presence, reference identity and coverage, exon/intron counts, extra copies; rnl/rns by blastn of the Af293 rRNAs; circularity from a terminal repeat (≥ 50 bp) on a single contig; a read subsample (`--organelle_reads` 1 M pairs / `--organelle_reads_ont` 50 k reads) mapped to mito + nuclear gives the mito/nuclear depth ratio (copy number) and, from a pileup, heteroplasmic sites (minor allele ≥ 10 % at ≥ 20×); output `organelle.json` + `<id>.mito.gff`. GetOrganelle/oatk were not added: they need SPAdes and Bowtie2 in the base image (or a further image) for a mitogenome the assembly already contains; MITOS was not added: its conda recipe pulls R, ViennaRNA and an older Biopython that conflict with the base environment — the gene-level annotation here uses the same reference proteins, and tRNAs are not annotated (documented). Master columns appended: `mito_size_kb`, `mito_core_genes` (n/15), `mito_circular`, `mito_copy_ratio`, `mito_heteroplasmic_sites`. Stub DAG 64 tasks; read gate skips 19 stages. Real-tool check in the base image on the local CEA10 assembly (v0.1 Mac run): stage 04 separated three AT-rich contigs (23.3, 23.2 and 29.4 kb, GC 25 %) each carrying 14–15 core genes while the 2.5 Mb chromosome with one NUMT-like hit stayed nuclear; stage 04b found 15/15 core genes at 96–100 % identity to Af293 (atp8/atp9/nad4L matched best through the *C. neoformans* references), a single cox1 intron of 2,011 bp (Af293: 2,020 bp), rnl and rns at 100 %, and flagged the contigs as redundant fragments of the circle (every gene on all three), so the size estimate is the longest contig, 29,364 bp (Af293 30,696 bp); a 50 k-read ONT subsample gave 294× on the mitogenome against 9× nuclear (copy ratio 32) and 41 candidate heteroplasmic substitutions at the 20 % ONT floor, mostly at low-depth contig ends. Found on the way: `samtools mpileup` with a reference applies BAQ realignment, which on kilobase ONT reads exhausts memory and returns nothing — the stage runs it with `-B` on a BAM downsampled to `--organelle_pileup_depth` × (default 300, 0 = all reads, for large-memory nodes), and indels are excluded from the heteroplasmy test because ONT homopolymer errors would dominate. **Pending for `validated`:** CEA10 on the dev deployment: one circular ~31 kb contig with 15/15 core genes (Af293 is 30,696 bp), copy ratio in the tens; DF-005 (Illumina) mitogenome separated and annotated.

#### W2.8 Mobile elements and mycovirus
**Design.** Stage geNomad's database; run on the whole assembly (plasmids/proviruses, EVEs) and the mitogenome; RepeatMasker `.tbl` parsed into `te_percent` and class breakdown; mycovirus screen by DIAMOND blastx of unplaced/low-coverage contigs against RVDB-prot, with the DNA-only caveat stated. **Validation.** `te_percent` populated for both isolates; geNomad status `ok`.

**As built (2026-09-17).** *TE landscape:* stage 06 now emits RepeatMasker's `.tbl` summary and `bin/repeatmasker_tbl.py` parses it into `te_percent` (bases masked), the interspersed-repeat fraction and the class breakdown (LTR with Gypsy/Copia, LINE, DNA transposons, Helitron, unclassified, simple repeats) — on the CEA10 table 4.67 % masked, 2.93 % LTR (Gypsy 2.29 %); the RepeatModeler family counts stay. *geNomad:* `fetch_references.sh genomad` stages database v1.9 (Zenodo, 0.8 GB, the version geNomad 1.12 expects); stage 10 runs `end-to-end` on nuclear + mitochondrial contigs together (`--genomad false` to disable), and `mobile_merge.py` reads the virus and plasmid summaries (counts, proviruses, taxonomy). *Mycovirus / EVE screen:* the RVDB-prot release already staged (`rvdb.fasta.xz`, 786,371 proteins) is turned into a DIAMOND database inside the task, every contig is searched by blastx (`--rvdb_evalue`, `--rvdb_sensitivity`), and `bin/mycovirus_screen.py` merges hits into loci and classifies each by the virus name in the RVDB title — mycovirus families (partiti-, toti-, chryso-, hypo-, narna-/mito-, endorna-, megabirna-, quadri-, botrex-/alphaflexi-, polymyco-, fusari-, yadokari-, ourmia-, alterna- and fungus-named viruses), retroelement-like (LTR retrotransposons, retrovirus-like, reported apart because RepeatModeler owns them), other viral (phage-like, usually contamination or spurious); `n_mycovirus` is now the number of mycovirus-family loci (endogenous viral element candidates; DNA-only caveat in every output). *Mitochondrial mobile introns:* `bin/mito_heg.py` translates six-frame ORFs (≥ 100 aa, genetic code 4) of the mitogenome and searches the Pfam LAGLIDADG_1/2/3, GIY-YIG and HNH profiles from the funannotate database → homing-endonuclease ORF count by family. Master columns appended: `te_ltr_pct`, `n_genomad_virus`, `n_genomad_plasmid`, `n_mito_heg` (`te_percent` and `n_mycovirus` are now populated). L15 closed: geNomad has a fetch step and runs on the whole assembly, RVDB is consumed. Real-tool check in the base image on the local CEA10 assembly: the RVDB DIAMOND database (786,371 proteins) built in 4 s from the staged `.xz`, blastx of the 30 Mb assembly took 36 s on six emulated cores, 108 hits merged into 30 loci — one mycovirus-family locus (a 1.5 kb region of a nuclear contig at 81 % identity to a partitivirus coat protein: an endogenous partitivirus element, which *A. fumigatus* genomes are known to carry) and 29 other-viral loci (large-DNA-virus-like protein families, not mycoviruses); the RepeatMasker table gave `te_percent` 4.67 (LTR 2.93, Gypsy 2.29, Copia 0.32, DNA transposons 0.83); 81 mitochondrial ORFs ≥ 100 aa were found but not scanned locally (Pfam-A.hmm lives with the funannotate database on the cluster). **Pending for `validated`:** dev-deployment runs after `fetch_references.sh genomad`: `te_percent` for CEA10 (≈ 4.7 %) and DF-005, geNomad status `ok`, the RVDB screen's categories on both isolates.

#### W2.9 Novelty
**Design.** Stage a representative fungal genome set (RefSeq representative genomes plus type-strain genomes for genera in the study, via NCBI datasets, listed in the manifest); skani ANI against it; novelty from ANI (< 95 % to nearest = candidate novel species, 95–97 % flagged) combined with ITS distance and the secondary loci. **Validation.** CEA10/DF-005 known; a synthetic divergent genome flagged novel in a unit test.

**As built (2026-09-17).** *Reference set:* `bin/fetch_reference_genomes.py` lists, through the NCBI Datasets API, the reference/representative genome of every species in the genera of `fungiforge/resources/novelty_genera.txt` (101 clinically and environmentally relevant genera; `GENOMES_GENERA` / `--genera` override, `GENOMES_ACCESSIONS` / `--accessions` add explicit genomes) and downloads each as `<accession>.fna` with a manifest (organism, species, strain, RefSeq category, level, length); `fetch_references.sh genomes` runs it (on request: tens of GB; the whole fungal reference set would be ~5,500 genomes). *Stage 12:* skani against every staged genome (`--rl` list, `--min-af` = `--novelty_min_af` 15 %), then `bin/novelty_call.py` applies the rule: ANI ≥ 95 % → `known_species` (the nearest genome's species; `identification_disagrees` flag when stage 08 named another species); 90–95 % → `candidate_novel_species` (sister lineage of the nearest species) unless ITS and a secondary locus agree at high confidence with that species (broad intraspecific ANI; basis states it); < 90 %, or no reference with enough aligned fraction → `candidate_novel_or_unrepresented` (a novel lineage, or a genus the set lacks — the manifest shows which); ITS-distance fallback without a genome set, `undetermined` without ITS. Output lists the five nearest genomes with ANI and aligned fractions. L16 closed. Real-tool check: the fetcher downloaded Af293 (GCF_000002655.1, 28 MB) and *A. flavus* NRRL 3357 (36 MB) with their manifest rows; skani of the local CEA10 assembly against both took 0.4 s and gave 99.59 % ANI to Af293 (aligned fraction 94 %), the *A. flavus* genome stayed below the 15 % aligned-fraction floor, and the verdict was `known_species`. **Pending for `validated`:** dev-deployment runs after `fetch_references.sh genomes`: CEA10 and DF-005 `known_species` with their nearest reference at > 98 % ANI.

### Phase 3 — Cohort features

#### W3.1 Phylogenomics and clonality
**Design.** Post-isolate stage: BUSCO single-copy orthologs across isolates → MAFFT → trimAl → IQ-TREE species tree; within same-species clusters, reference-based SNP calling (minimap2/bwa + bcftools, MycoSNP-style filters) → pairwise SNP distances and clonal groups at a documented threshold; outputs to `results/cohort/`. **Validation.** CEA10 vs C87 vs C6/E142 tree topology matches published relationships; unit tests on distance matrices.

**As built (2026-09-17).** New run-level stage 16 `COHORT` (`modules/stage16_cohort.nf`, `bin/cohort_phylo.py`, base image; `--skip_cohort`) over every isolate that passed the gates, writing `<outdir>/cohort/`. (1) *Species clusters:* `skani triangle` all-vs-all → `ani_matrix.tsv`; single-linkage clusters at `--cohort_ani_cluster` (95 %) with an aligned-fraction guard → `species_clusters.tsv` with the stage-08 species of each member. (2) *Phylogenomics:* stage 05 now emits each isolate's single-copy BUSCO proteins (`bin/busco_singlecopy.py`, compleasm or BUSCO layout); genes single-copy in ≥ `--cohort_min_frac` (80 %) of the isolates (≤ `--cohort_max_genes`) are aligned with MAFFT, gap-trimmed (columns with > 50 % gaps dropped), concatenated into `cohort_alignment.faa` with a partition file, and IQ-TREE 3 (LG+G, 1000 ultrafast bootstraps; `--cohort_tree_method fasttree` for FastTree) gives `cohort.treefile`; below `--cohort_min_isolates` (4) the tree is skipped with the reason. (3) *Clonality:* within each cluster of ≥ 2 isolates the most complete assembly (BUSCO, then N50) is the reference; every member is aligned to it (minimap2 asm5 `--cs`) and `paftools.js call` gives the aligned regions and variants; the core is the reference span aligned in every member, the pairwise distance the number of core SNVs at which two members differ (indels excluded: the ONT/homopolymer error mode); `snp_distances.<cluster>.tsv`, and single-linkage clonal groups at `--clonal_snp_threshold` (100 core SNVs; the threshold is a parameter because fungal outbreak thresholds vary by species and are reported alongside the distance distribution) → `clonal_groups.tsv`; `cohort.json` carries everything with a note that assembly-based SNVs from ONT-only assemblies carry residual error. Read-based MycoSNP-style calling was not implemented: the assembly route needs no per-isolate read mapping to a common reference, and hybrid/Illumina assemblies are base-accurate; it is noted as the upgrade path if ONT-only cohorts need finer resolution. L17 closed. Real-tool check in the base image on a three-genome mini cohort (the local CEA10 assembly, Af293 and *A. flavus* NRRL 3357): skani clustered CEA10 with Af293 (99.59 % ANI) and NRRL 3357 apart; the CEA10–Af293 core spanned 27.4 Mb with 42,457 SNVs (1.55 per kb, the published divergence of the two strains) and no clonal group at the 100-SNV threshold; the tree was skipped with its reason (no BUSCO proteins, fewer than four isolates); 4.8 s in total. **Pending for `validated`:** the study batch on the dev deployment (species clusters vs identifications, tree, SNP distances within the *A. flavus* cluster) and the published CEA10 / C87 / C6 / E142 topology.

#### W3.2 Cohort BGC families and mycotoxin flags
**Design.** BiG-SCAPE over all region GenBanks with MIBiG; families table; mycotoxin flags from KnownClusterBlast hits to curated MIBiG entries (aflatoxin, ochratoxin, fumonisin, gliotoxin, patulin, trichothecene, ergot). **Validation.** DF-005 flags the aflatoxin cluster; CEA10 flags gliotoxin.

**As built (2026-09-17).** *Mycotoxin flags (stage 11):* the CEA10 antiSMASH JSON showed that KnownClusterBlast had never been enabled, so stage 11 now passes `--antismash_extra` (default `--cb-knownclusters`) and `bin/bgc_summary.py` reads the `antismash.modules.clusterblast` results: the best MIBiG cluster per region (accession, description, type, core-gene hits, BLAST score, proteins hit) and flags from `fungiforge/resources/mycotoxin_compounds.tsv` (31 compound patterns in three classes: mycotoxin — aflatoxin, sterigmatocystin, ochratoxin, fumonisin, gliotoxin, patulin, trichothecenes, ergot alkaloids, citrinin, zearalenone, fumagillin, fumitremorgins, cyclopiazonic acid, tenuazonic acid, alternariol, beauvericin/enniatin, fusaric acid, fusarins, aspergillic acid, penitrems, roquefortines; antifungal/antibiotic — penicillin/cephalosporin, echinocandins, griseofulvin; other bioactive — pseurotin, helvolic acid, fumiquinazoline, trypacidin, lovastatin, cyclosporin). `bgc.json` gains `knownclusterblast.best_hits`, `mycotoxin_flags`, `mycotoxin_compounds`, `bioactive_compounds`; master columns appended: `mycotoxin_clusters`, `bioactive_clusters`, `n_bgc_mibig_hits`. *Families (stage 16):* `bin/bgc_families.py` parses every isolate's region GenBank, runs DIAMOND all-vs-all on the region proteins and links two regions into a gene-cluster family when at least `--gcf_min_similarity` (50 %) of the smaller region's proteins have a homologue (≥ `--gcf_min_pident` 50 % identity, ≥ 50 % query coverage) in the other, single linkage; `cohort/bgc_families.tsv` and `.json` list members per isolate, product types and the best MIBiG description per family. BiG-SCAPE 2.0.3 was then added to the base image after the dry-solve showed it costs only ~10 MB of extra dependencies, and it is now the **primary** method (`--gcf_method auto`): `bin/bgc_families.py` splits each isolate's regions into per-region GenBanks, runs `bigscape cluster` with the funannotate database's `Pfam-A.hmm` (pressed locally when the staged copy is read-only) at `--gcf_cutoff`, and reads the per-class `*_clustering_c*.tsv`; the shared-protein clustering remains the fallback when BiG-SCAPE or Pfam is unavailable. Real-tool check in the base image: the 40 antiSMASH regions of the local CEA10 run, duplicated as a second isolate, gave 80 regions → exactly 40 families, each containing the two copies of one region and none merging distinct regions (DIAMOND all-vs-all of 1,000 region proteins, 1.3 s). L18 closed. **Pending for `validated`:** dev-deployment runs (antiSMASH with KnownClusterBlast): DF-005 flags aflatoxin, CEA10 flags gliotoxin (and fumitremorgin, fumagillin/pseurotin, helvolic acid, trypacidin as known *A. fumigatus* clusters); the batch's shared families.

#### W3.3 Reporting
**Design.** Per-isolate HTML report (Jinja2) with QC, identification, resistance, BGC and mobile summaries; cohort MultiQC; the pipeline writes a merged `results/master_fungi.tsv` via `collectFile`; a JSON schema for the master table checked in the report stage; Layer-2 scripts updated for new columns. **Validation.** Schema validation in tests; Layer-2 runs end-to-end on the batch output.

**As built (2026-09-17).** *Schema.* `fungiforge/resources/master_schema.json` declares all 61 master columns (name, type, enum where applicable, description, group) and states the contract: append-only, `NA` for anything not measured, `sample` unique. `bin/validate_master.py` enforces it — type and enum checks per cell, duplicate and empty sample ids, missing columns as errors, unknown trailing columns as a warning so a newer table still validates against an older schema. A unit test asserts the schema and `MASTER_COLS` never drift apart. *Per-isolate report.* `bin/make_report.py` now renders `fungiforge/report_templates/isolate_report.html.j2` (Jinja2, autoescaped): headline call and gate, the resistance table with evidence tier and read support per call, reads/assembly, per-locus identification evidence, annotation and biology, secondary metabolism with the mycotoxin flags, mobile elements and the mitochondrion, run status; the previous inline layout stays as a fallback so a missing template can never fail a run. *Cohort.* New run-level stage 17 `SUMMARY` (`bin/cohort_report.py`): merges every isolate's row into `results/04_summary/master_fungi.tsv` in schema order, validates it, and writes `cohort_report.html` (composition, gates and QC, resistance and mycotoxin tallies, metric ranges, and stage 16's clusters, tree, clonal groups and BGC families), `cohort_summary.json`, and MultiQC custom content (`multiqc/fungiforge_mqc.json` table plus bar-plot sections and a config), running MultiQC itself when present — `multiqc==1.25.1` joined the base environment for that. *Layer 2.* `01_merge_metadata.R` now reads the merged table (falling back to per-isolate rows) and copies the cohort tables next to the analysis dataset; `02_recover_metadata.R` derives the v0.2 columns (mycotoxin carriage and the aflatoxin/gliotoxin flags, gate outcome, identification confidence, read-confirmed resistance, TE/mycovirus/secretome/CAZyme/mitochondrial numbers) and tolerates their absence in an older results directory; objective 4 reports genome-level clonal groups and their cross-compartment spread from `clonal_groups.tsv` instead of candidate pairs alone; objective 7 builds the shared and cross-compartment gene-cluster families from `bgc_families.tsv`. L19 and the Layer-2 half of L23 closed. **Pending for `validated`:** the batch cohort report on the dev deployment and Layer 2 run end to end against the merged table.

### Phase 4 — Engineering and documentation

#### W4.1 Hygiene and documentation pass
Samplesheet and metadata schema validation with clear errors; `nextflow lint` clean; shellcheck clean; manual rewritten to describe actual behaviour with a roadmap section; CHANGELOG; CITATION updated; v0.2.0 release notes.

**As built (2026-09-17).** *Samplesheet validation (L21).* `bin/validate_samplesheet.py` (`fungiforge check --samplesheet`) is the pre-flight: required header, sample ids present, unique and safe for file names and tool arguments, ONT alone / both Illumina mates / both, files that exist and are not empty (relative paths resolved against the launch directory and then the sheet's own directory), a warning when the same file is given as both mates, unknown columns listed, and a metadata summary that flags values differing only in case or spacing (`AIR` vs `Air`) before a batch runs — every problem reported, not just the first; `--strict` turns warnings into failures, `--no-check-files` checks structure alone. `main.nf` gained the cross-row guard the per-row parser could not make: duplicate or malformed sample ids now fail at launch with the pre-flight command in the message (duplicates would have silently overwritten each other's results). `fungiforge check-master` exposes the W3.3 master-table validator the same way. *Haplotig purging (L30).* purge_dups ran inside the Flye image, which carries neither purge_dups nor minimap2, so it exited 127 on every long-read isolate and the assembly stage was permanently `partial`; it is now stage 02c in the base image (`purge_dups==1.2.6` added), skipped cleanly for short-read isolates, and reports how many haplotigs it removed. *Install safety.* `bin/hpc_install.sh` refuses to build the base image from a stale lock: when `env/base.yml` is newer than `env/base.linux-64.lock` it builds from the yml, says so, and asks for `bin/lock_env.sh` to be run against the resulting image — a trap that would otherwise have shipped an image silently missing nQuire, BiG-SCAPE, MultiQC and purge_dups. The install smoke check now exercises those four tools. *Documentation.* `CHANGELOG.md` written (0.1.0 → 0.2.0 with the fixed defects), `CITATION.cff` bumped, and the manual sections rewritten as each stage changed (the "honest limitation" notes for the mitochondrial split, mycoviruses and extras are gone because the stages now do the work, L22). **Pending for `validated`:** the release-notes half rides with W5.1 (tag v0.2.0).

#### W4.2 Databases, installer, site config
Fetch script gains geNomad, reference genome set, marker-locus references, InterPro data, eggNOG, SignalP model placement; Apptainer path for the container-driven downloads; installer and site config updated for new images and resources; deployment guide updated.

**As built (2026-09-17).** The fetch steps were added with the items that needed them: `markers` and `mlst` (W2.3), `genomad` (W2.8), `genomes` (W2.9), `dbcan`, `phibase`, `effectorp` (W2.6), `interproscan` (W2.5), multi-lineage `busco` by direct download instead of a container (W2.3), and eggNOG as a direct download after the container route failed on the cluster (W2.5). W4.2 closed the last gap (L24): `crun` in `bin/fetch_references.sh` runs the two container-built databases (funannotate `setup -i all`, `download-antismash-databases`) under **Apptainer or Singularity** when present — pulling into the shared image cache, bypassing the image entrypoint — and under Docker otherwise, so a cluster with no Docker daemon can stage everything; with no runtime at all it fails with one clear line. The installer stages the licensed SignalP 6 image (`--signalp`) and writes the `extras` label into `site.config`, pre-pulls the two new public images (eggnog-mapper, interproscan — picked up automatically from `conf/base.config`), refuses a stale environment lock, and its smoke check exercises nQuire, BiG-SCAPE, MultiQC and purge_dups. `docs/hpc_deployment.md` now carries the full database table with sizes and the two licensed resources.

### Phase 5 — Deployment

#### W5.1 Cluster upgrade and re-validation
After production runs complete: merge `develop` → `main`, tag `v0.2.0`, `hpc_install.sh --ref v0.2.0 --rebuild-images`, rerun CEA10, DF-005 and all controls, run `validate_run.py`, update the deployment document and `docs/validation.md`.

### Phase 6 — v0.3 candidates (gaps identified by the 2026-09-20 assessment)

Recorded, not started. Each is sized against what the v0.2 evidence actually shows; the ordering
is by demonstrated value, not by interest. Nothing here blocks the v0.2 release.

#### W6.1 Scheduled real-data regression *(highest value; process, not features)*
**Evidence.** Ten result-affecting defects were found between 18 and 20 September by one person
watching a six-hour run; six were silent (the run reported success while producing wrong or missing
results). Unit tests and stub runs structurally cannot catch them: a stub never executes a task
script, and a hosted runner cannot stage a 22 GB database. **Goal.** Catch this class within a week
of a commit instead of at the next manual validation. **Design.** A downsampled CEA10 (≈ 20× ONT +
10× Illumina, whole pipeline, ~1 h) run weekly on the cluster against `develop` from a cron or
SLURM timer, ending in `validate_run.py --expected test/expected/AfumCEA10.subsampled.json` and a
one-line pass/fail appended to a log the next session reads; a non-zero exit posts the failing
stage JSON. Requires a new expected file with tolerances widened for the lower depth.
**Validation.** Replay the four most recent silent defects against it (mitogenome purged, `CDS`-only
annotation filter, RVDB extension, nQuire on a haploid) and confirm each fails the check.

#### W6.2 RNA-seq evidence path, and the funannotate 2 decision
**Evidence.** `L25` recorded the missing RNA-seq path. The comparison confirms every serious fungal
annotator uses transcript evidence (FunGAP requires it; funannotate supports `train`/`update`), and
that funannotate's v1 line has had no release since 2024-03-01 while `funannotate2` has released
through 2026. **Goal.** Gene models from evidence, not `ab initio` alone, and a decision on the
upstream line. **Design.** Optional `rna_r1`/`rna_r2` samplesheet columns; when present, stage 07a
runs `funannotate train` (HISAT2/Trinity/PASA inside the existing image) and `funannotate update`
after annotate. Evaluate `funannotate2` on CEA10 in parallel before committing to it — the v1 image
stays pinned by digest either way. **Validation.** CEA10 and DF-005 gene counts, BUSCO protein
completeness and UTR coverage with and without RNA-seq; a published *A. fumigatus* RNA-seq accession
as the test input.

#### W6.3 Resistance mechanisms beyond substitutions
**Evidence.** FungAMR carries 35,792 entries including deletions, aneuploidy, overexpression and
gene disruption; `fungamr_panel.py` derives 5,347 substitution entries from it. ChroQueTas, the
reference FungAMR implementation, covers 8,285 substitutions and explicitly excludes CNV,
aneuploidy, overexpression and disruption. **Goal.** Report the mechanisms the catalogue encodes
that substitutions cannot express. **Design.** Aneuploidy from the per-contig depth already computed
in stage 09 plus the nQuire result (whole-chromosome depth ratio against the genome median);
gene amplification from the existing `copy_number` ratio, promoted from a flag to a reported call;
gene disruption from premature stops and frameshifts already visible in the GenBank. Overexpression
needs transcript evidence and therefore waits on W6.2. **Validation.** A known *C. albicans*
aneuploid and a known *C. glabrata* amplification from public data; CEA10 and DF-005 stay negative.

#### W6.4 Chromosome-scale assessment: scaffolding and telomeres
**Evidence.** The other half of `L25`. CEA10 now assembles to 29 contigs at N50 2.47 Mb, close
enough to chromosome scale that contig count alone understates it. **Goal.** A completeness
statement rather than a contig count. **Design.** Telomere-motif counting at contig ends
(`TTAGGG`-class repeats, species-configurable) in stage 05, and optional reference-guided
scaffolding (RagTag) against the staged reference genome set behind `--scaffold true`, never
overwriting the primary assembly. New master columns `n_telomeres`, `n_t2t_contigs`.
**Validation.** CEA10 against the Af293 chromosome assembly; the scaffolded result must not change
BUSCO completeness or gene counts.

#### W6.5 Typing resolution beyond seven loci
**Evidence.** MLST gives `afumigatus ST5` for CEA10, which is correct and coarse for outbreak work.
Core-SNP clonality is already implemented within an ANI cluster (W3.1), so the gap is narrower than
it looks. **Goal.** Discriminate isolates that share an ST. **Design.** Size it before building:
compare the existing assembly-based core-SNP distances against a cgMLST scheme on the production
batch, and only add a scheme if the SNP distances prove insufficient. If they do, a cgMLST allele
caller against a published *A. fumigatus* scheme, reported as `cgmlst_profile` and used to refine
`clonal_group`. **Validation.** Isolates known to share ST5 must separate on core SNPs or cgMLST.

#### W6.6 RNA mycovirus detection
**Evidence.** The screen is DNA-based and documented as such; RNA mycoviruses appear only as
endogenous viral elements. The field's own tooling (DiscMycoVir, 2025) is transcriptome-only.
**Goal.** Detect RNA mycoviruses where transcript data exist. **Design.** Reuse the RNA-seq input of
W6.2: assemble unmapped reads, screen the contigs against RVDB-prot with the existing classifier.
Strictly dependent on W6.2 and pointless without it. **Validation.** A published mycovirus-positive
*A. fumigatus* transcriptome.

#### W6.7 External validation and publication
**Evidence.** Every comparator with users outside its own group has a paper; FungiForge has neither.
The 2026-09-20 assessment found no published pipeline combining assembly, polishing, multi-locus
identification, MLST, resistance genotyping, gene clusters, mycovirus screening, cohort phylogeny
and a schema-validated master table. **Goal.** External scrutiny, which so far has been supplied
entirely by running the pipeline on real data. **Design.** A methods description with the v0.2
validation record, the limitation inventory as an honest limitations section, and the master-table
schema as the contribution most reusable by others. **Validation.** A second site reproduces the
CEA10 expected-results file from the deployment guide alone.

#### W6.8 Small, cheap, already specified
- Surface the cohort tree's four-isolate minimum in the report, not only in a JSON field.
- Default `--interproscan_apps` to `Pfam,PANTHER,SUPERFAMILY`; the full seventeen member databases
  dominate runtime for most users and the rest rarely change a conclusion.
- Push the regenerated `env/base.linux-64.lock` from the cluster: the reproducibility artefact W0.2
  exists to provide is currently one commit behind the images it describes.

## Appendix A — Licensed software

**GeneMark-ES/ET/EP+** (Georgia Tech). Free for academic use. Request at `http://exon.gatech.edu/GeneMark/license_download.cgi`: choose "GeneMark-ES/ET/EP+" for LINUX 64, fill in name, institution and email, accept the licence; download `gm_key_64.gz`, then `gunzip gm_key_64.gz && cp gm_key_64 ~/.gm_key` on the machine that runs annotation, or point `--genemark_key` at it. Keys expire roughly yearly; renew the same way. **The GeneMark binaries are not in the funannotate image** (checked 2026-09-17): download `gmes_linux_64_4.tar.gz` from the same page, unpack it somewhere the containers can see (on the cluster, under `/hpc/opt/licensed/genemark/`), and pass `--genemark_dir <unpacked dir> --genemark_key <gm_key>`; the profiles bind the directory into the container and stage 07a exports `GENEMARK_PATH`. Its Perl prerequisites (YAML, Hash::Merge, Logger::Simple, Parallel::ForkManager, MCE) are present in the image.

**SignalP 6.0** (DTU Health Tech). Free academic licence: `https://services.healthtech.dtu.dk/services/SignalP-6.0/`, "Downloads", register with an academic email, download the `signalp-6.0*.fast.tar.gz` package and build the site image with `bin/hpc_install.sh --signalp <tarball>` (env/signalp6.Dockerfile; the image stays on site). The slow_sequential package works identically, ~10× slower. EffectorP 3, dbCAN3, nQuire, Smudgeplot, GetOrganelle, MITOS2, geNomad, BiG-SCAPE and InterProScan are free and need no registration.

## Change log of this document

- 2026-09-20 — assessment of where v0.2 stands, recorded as Phase 6. Thirty of the thirty-one catalogued limitations are closed; `L25` was out of scope from the start. A verified survey of the published landscape found no pipeline combining assembly, polishing, multi-locus identification, MLST, resistance genotyping, gene clusters, mycovirus screening, cohort phylogeny and a schema-validated master table; the closest, rMAP-Candida and FunFlux (both 2026), each cover about half, and no tool found does TR34/TR46 promoter detection from whole-genome data. Eight v0.3 candidates added (W6.1–W6.8), led by a scheduled downsampled real-data run: ten result-affecting defects in three days, six of them silent, were all found by a person watching a six-hour run, which is the one class of failure unit tests and stub runs cannot reach.
- 2026-09-20 — every mitochondrial column on the CEA10 run was NA, and the cause was the W4.1 purge_dups fix. purge_dups scores contigs by read depth against the nuclear peak; a mitogenome sits far above it and is dropped as collapsed duplication. It removed nine of the ten organelle-shaped contigs, including the 30,662 bp / 25.5 % GC mitogenome, so stage 04 had nothing to separate and stage 04b, the HEG scan and five master columns were all empty. This was invisible before because purge_dups exited 127 and purged nothing (L30). New `bin/protect_organelle.py` reclassifies the PRE-purge assembly with the same tblastn core-gene evidence stage 04 uses and restores only contigs that meet it; stage 02c runs it after get_seqs and records `n_organelle_restored`. Six unit tests cover restoration, the `_1` rename get_seqs applies, a haplotig without core genes, a single-gene hit, and a long high-GC NUMT that must not drag a chromosome back. Separately, the mycovirus screen was skipped because the stage accepted only `rvdb.fasta.xz` while the staged file was `rvdb.fasta.gz`; all three forms are now accepted and the fetch step keeps the release's own compression in the name.
- 2026-09-20 — CEA10's master row reported `ploidy 4` for a haploid organism. nQuire had 2,491 biallelic sites over 30 Mb (about one per 12 kb) and returned its best fit, tetraploid, while the k-mer profile of stage 01b said haploid. The guard existed but sat at 1,000 sites. Density now decides: below `--ploidy_min_sites` (10,000 by default, configurable) the call is reported haploid/homozygous at low confidence with nQuire's own best fit kept in the JSON, and `ploidy_confidence` records which applies. Also confirmed from the run: `n_secreted` and `n_effectors` are NA because the SignalP 6 site image was never built (`signalp6: command not found`), which needs `hpc_install.sh --signalp <tgz> --rebuild-images` on the dev deployment.
- 2026-09-19 — root cause of the NA annotation-coverage columns: `annotate_stats.py` kept only rows whose `Feature` column reads `CDS`, but funannotate 1.8.17 writes `mRNA` (the table is per transcript). Every row of the real 9,844-gene table was discarded, `n_annotated` was 0, and pct_pfam / pct_go / pct_eggnog / pct_interpro all came out NA even though eggNOG and InterProScan had run. The filter now excludes the non-coding types instead of whitelisting one label. The unit fixture had hard-coded `Feature: CDS`, which is why the tests passed while real data failed; it now writes `mRNA` and the label handling is tested in both directions. Also confirmed from the run: the master table is exactly 61 fields on header and row, so there was no ragged-row defect.
- 2026-09-19 — CEA10 completed all 27 stages on v0.2.0 and passed 21/21 expected-result checks (29.65 Mb / 28 contigs / N50 2.47 Mb, BUSCO 99.7 %, *A. fumigatus* high confidence on ITS+CaM+BenA+genome, ST5, 9,844 proteins with GeneMark and the eurotiomycetes set, 54 BGCs, no resistance, novelty known_species). Open gap: the four annotation-coverage columns came back NA although eggNOG and InterProScan both ran, so `annotate_stats.py` now records WHY a table yielded nothing (missing / empty / the header it actually saw) instead of returning an empty dict that is indistinguishable from a stage that never ran, and tolerates a table without the `Feature` column rather than filtering every row away. Hardening found alongside: master values are now whitespace-collapsed before being written, since a tab inside a tool-derived value would silently add a field; `validate_master.py` reports rows with more or fewer fields than the header, which `csv.DictReader` accepted silently; and reading the header from the file rather than from `rows[0].keys()` fixes a TypeError the validator raised on exactly such a row.
- 2026-09-19 — stage 16 COHORT failed five hours into the CEA10 run with `FileNotFoundError: cohort.json`, after skani, MAFFT, IQ-TREE, minimap2, DIAMOND and BiG-SCAPE had all succeeded (42 regions, 42 families). The block that folds the stage status into the published `cohort/cohort.json` read the status document before `ff_finalize` wrote it; `ff_finalize` now runs first and the merge is defensive about both files. Added `test/unit/test_module_status_order.py`, a static check that no module reads its `ff_init` status JSON before anything creates it — writing it first and reading it back is a legitimate pattern several stages use, so only genuinely premature reads are flagged, and the check is verified against the real regression in both directions.
- 2026-09-19 — CEA10 completed annotation, resistance, BGC and extras, then stage 16 COHORT tried to pull `aduton1000/fungiforge:0.2.0` from Docker Hub and failed (private repository). Cause: `share/site.config.example` repoints the base-image labels at the locally built `.sif` by listing them, and `cohort` — added in W3.1 — was never added to that list, so it alone kept the Hub reference. The list is now derived from `conf/base.config` by `hpc_install.sh`, for a new site.config and for an existing one, and `test/unit/test_site_config_labels.py` asserts the template covers exactly the labels whose container is `params.fungiforge_image`.
- 2026-09-19 — CEA10 prediction succeeded with GeneMark (9,702 proteins, Augustus seed `aspergillus_fumigatus` from the species call), which validates W2.5's species-aware training path. Two gaps it exposed. `funannotate setup -i all` installs only the dikarya BUSCO set, so every clade set the training map can choose was missing and training fell back to dikarya with the reason recorded; the step now passes `-b all`. And `compute_status` returned `ok` for a stage where nothing ran and everything was skipped, so InterProScan reported success for writing an empty table when its data were absent; a stage with no tools run and at least one skip now reports `skipped`, which also means such stages appear in the `stages_failed` column rather than being invisible. A skip alongside real work (GeneMark absent during a successful prediction) is still `ok`.
- 2026-09-18 — CEA10 reached stage 07a and failed there: GeneMark reported `License key ".gm_key" not found` on every contig. Two causes, both fixed. The profiles bound `--genemark_dir` but not the directory holding `--genemark_key`, and the key normally sits beside that directory rather than inside it, so the key was invisible inside the container; all three engine profiles now bind the key's directory too. And `GENEMARK_PATH` was exported before the key was verified, so funannotate used GeneMark regardless and every licensing problem became a failed isolate instead of the documented fall back to Augustus alone; the checks are now ordered, each with its own reason, and the variable is exported only once the key is in place. HOME is a per-task directory (the container may provide none, or one shared by concurrent isolates writing the same `~/.gm_key`). A licence that is present but rejected, for instance expired, can only fail once prediction is under way, so prediction now retries once without GeneMark and records the stage partial rather than losing hours of work.
- 2026-09-18 — CEA10 validation run failed after Flye succeeded: stage 02 still wrote `purge_dups_applied` from `$PURGED`, a variable that moved to stage 02c with W4.1's haplotig-purging split, so the task died under `bash -ue` on the line after a 30 Mb assembly completed. Field removed (stage 02c owns it; nothing read it from the assemble JSON). The same class of defect was found in stage 07a, where `eval "$(annotation_training.py …)"` leaves AUGUSTUS_SPECIES and BUSCO_DB unset if the helper fails, surfacing far below as an unbound variable naming neither cause nor helper; the helper's exit status is now checked and the documented defaults applied (`--funannotate_seed`). Added `test/unit/test_module_shell_vars.py`: a static check that every module's `script:` block assigns every shell variable it references, parametrised per module, with a planted-regression case. Stub tests cannot catch this because they never execute the real script block.
- 2026-09-18 — W0.4 in progress; six robustness fixes found while bringing the dev deployment to a state where the CEA10 validation run could start alongside a database download. Stage 07b treats an eggNOG database that exists but is short (a staging run still downloading) as absent and runs emapper as an optional step, so a run launched during a fetch can no longer lose an isolate hours in. `fetch_references.sh` prefers `wget -c` over `curl -C -` for the multi-GB databases, because curl rewinds a retried transfer to byte 0 and the eggNOG file grew and shrank for hours without finishing; every archive is integrity-checked before extraction and deleted if it fails, and `--wait` queues a fetch behind a running one. The flock descriptor is closed for the download child, so killing a fetch actually frees the data directory instead of leaving the lock held by an orphaned `curl`. The CLI resolves the checkout from `FUNGIFORGE_HOME` rather than its own package location, which on a site install pointed every helper subcommand at `site-packages/bin/`, and `hpc_install.sh` installs it editable so a `git pull` moves the CLI with the pipeline. InterProScan is now enabled by `fetch_references.sh interproscan` plus `--run_interproscan true` alone: the fetch leaves a stable `data` symlink beside the versioned release and the engine profiles derive the bind from `--data_dir`, binding only when the directory exists.
- 2026-09-18 — version bumped to 0.2.0 (manifest, package, default image tag) so the dev deployment builds `fungiforge-0.2.0.sif` and `bin/lock_env.sh` resolves the right tag. Three installer fixes after the first cluster rebuild: `--signalp` (and every argument) is validated before the 35-minute image build instead of after it; the stale-lock check is by CONTENT (a `==` pin in `env/base.yml` missing from the lock) because a git checkout gives both files the same modification time, so the first rebuild silently used the stale lock and produced an image without BiG-SCAPE, MultiQC and purge_dups; and an existing `site.config` has its `.sif` paths repointed at the images the install actually built.
- 2026-09-17 — W4.2 built: container-driven database downloads run under Apptainer/Singularity or Docker (L24), deployment guide rewritten with the database table and the licensed resources; the individual fetch steps had landed with their own items.
- 2026-09-17 — W4.1 built: samplesheet pre-flight validation (`fungiforge check`) with duplicate/malformed ids failing at launch, haplotig purging moved to its own stage in the base image (L30), installer guard against a stale environment lock, CHANGELOG and CITATION for 0.2.0.
- 2026-09-17 — W3.3 built: master-table JSON schema and validator, Jinja2 per-isolate report, run-level stage 17 writing the merged master_fungi.tsv, cohort_report.html, cohort_summary.json and MultiQC custom content; BiG-SCAPE 2 became the primary gene-cluster-family method (W3.2) now that it fits the image; L19 closed.
- 2026-09-17 — W3.2 built: KnownClusterBlast enabled in stage 11 (it had never run), best MIBiG hit per region and mycotoxin / bioactive flags from a curated compound list, cohort gene-cluster families by shared-protein clustering in stage 16; three master columns; L18 closed.
- 2026-09-17 — W3.1 built: run-level cohort stage (skani ANI species clusters, single-copy BUSCO supermatrix with MAFFT + IQ-TREE 3, assembly-based core SNP distances and clonal groups per cluster), stage 05 emits single-copy BUSCO proteins; L17 closed.
- 2026-09-17 — W2.9 built: reference genome set staged from NCBI Datasets per genus of interest (manifest), skani against it in stage 12, novelty verdicts from ANI with the ITS/secondary-locus rescue and an unrepresented-genus category; L16 closed.
- 2026-09-17 — W2.8 built: RepeatMasker table parsed into te_percent and the TE landscape, geNomad database fetch step and whole-assembly run, RVDB-prot mycovirus/EVE screen with family classification, mitochondrial homing-endonuclease ORFs; four master columns; L15 closed.
- 2026-09-17 — W2.7 built: mitochondrial contigs separated in stage 04 by core-gene tblastn (bundled three-species reference set), new stage 04b annotates the mitogenome (core genes, introns, copies, rRNAs, circularity, copy number and heteroplasmy from a read subsample); five master columns; GetOrganelle/MITOS deliberately not added (image weight / dependency conflicts), reasons recorded.
- 2026-09-17 — W2.6 built: tool-backed extras (dbCAN HMMs, SignalP 6 site image + EffectorP 3, PHI-base, Pfam-based MAT locus typing with GenBank synteny, nQuire ploidy on the stage-09 BAM), nQuire compiled into the base image, three fetch steps, installer `--signalp`, four master columns.
- 2026-09-17 — W2.5 built: stage 07 split into predict (GeneMark from a bound licensed directory, species-aware Augustus seed + BUSCO set from the stage-08 call), eggNOG-mapper and InterProScan stages in their own images, annotate with coverage statistics; six master columns; eggNOG data fetched directly, InterProScan data step; Appendix A corrected (GeneMark is not in the funannotate image).
- 2026-09-17 — W2.4 built: FungAMR-derived panel with evidence tiers merged into the curated overlay, read-level genotyping of every hotspot (codon translation per read, zygosity, reads-only alleles), read-level TR check against the assembly, copy-number ratio, combined confidence, two master columns; stage 09 maps a read subsample to the GenBank records; tests, in-image check on the real cyp51A locus.
- 2026-09-17 — W2.3 built: secondary loci (CaM/BenA/TEF1/RPB2 by tblastn, LSU D1/D2 from barrnap) searched against NCBI type-material sets, concordance rule with ties and discordance flags, PubMLST fungal MLST, species-aware BUSCO stage 08b with a genus→lineage map, five master columns, fetch steps `markers`/`mlst` and multi-lineage `busco`, tests and docs.
- 2026-09-15 — created; limitation inventory L1–L25; items W0.1–W5.1 defined; all `todo`.
- 2026-09-17 — W2.2 built: trimmed reads routed downstream, read_stats in readqc.json, READ_TRIAGE (stage 00b) with the read-level gate, KMER_PROFILE (stage 01b) with kmc + genomescope2 in the base image (lock refreshed), five new master columns, tests and docs.
- 2026-09-16 — W2.1 built: the gate after assembly QC (GATE process, `gate` master column, `--force_all`, stub hooks, nf-test cases); Medaka model resolved from read headers with validation (L27); Polypolish change count recorded (L28 first half).
- 2026-09-16 — W1.1 built: validate_run / benchmark_assembly / control_genotype tools with tests, expected files for CEA10 and DF-005, PRJNA1494181 controls chosen with read-level verification, fetch_controls.sh, docs/validation.md; phantom QUAST step removed (L29); *Candida* controls deferred to W2.4.
- 2026-09-16 — Node /tmp tmpfs incident, third round: v0.1.1's TMPDIR export did not reach funannotate-p2g.py (hard-coded /tmp); v0.1.2 bound a work-dir directory over /tmp for the annotate task; the full tmpfs then broke barrnap (ENOSPC, exit 28) in the dev CEA10 run — visible only because of the W0.1 contract. v0.1.3 / develop: EVERY containerised task gets its own /tmp (process-level `beforeScript 'mkdir -p tmp'` + `containerOptions` bind) under docker/singularity/apptainer; verified with Docker on plain and antiSMASH (entrypoint-cleared) tasks.
- 2026-09-15 — C87 positive control run (production v0.1.0, 15/15 stages): cyp51A Y121F + T289A + G448S at high confidence, promoter wild-type. Read-level check showed the deposited reads genuinely lack TR34 while the published assembly has it; the detector called TR34 on the published assembly (fixture `test/fixtures/cyp51A/`). Added L26–L28. The production batch also hit a node-tmpfs overflow in annotation (v0.1.1 hotfix on main, ported here).
- 2026-09-15 — W0.3 built: 127 unit tests over every helper and the CLI (subprocess coverage 93 %), `nextflow lint` zero warnings (tasks call `bin/` helpers via PATH, no `projectDir` in processes), shellcheck clean, nf-test suite (routing + 19 module stubs + DAG, 28 cases), Docker fixture script, GitHub Actions CI. Real bugs fixed on the way: `id_classify` species-level calls on `Genus_sp` hits, `af_resistance` "spp." matching every genus, duplicate FungAMR accessions double-counted. Nightly Docker CEA10 job dropped as infeasible on hosted runners (documented).
- 2026-09-15 — W0.2 built: pinned image tags + digests, exact conda pins + explicit lock (image builds install from it), `ff_version`, `DB_CHECK` and `PROVENANCE` run-level stages, schema-validated `provenance.json` with image sha256/digests, basecall stage onto the status contract, 23 new unit tests, stub DAG with and without a container engine, lock reproducibility verified by two builds. Found on the way: the local 0.1.0 image lacked kraken2 (lock check), `dfam/tetools:1.99` is older than the validated `latest` (now `2.00`), RVDB is fetched but unused (noted under L15).
- 2026-09-15 — W0.1 built: `bin/ff_status.sh` + `bin/ff_status.py`, all 15 modules converted (no `|| true` masking a tool), assemble/medaka status JSONs, `stages_failed` master column, 25 unit tests, Docker fixture run exercised ok and failed paths. Found and fixed in the process: a bare optional `ff_run` tripped `bash -ue` errexit (now returns 0 and sets `$FF_RC`). Added W0.4 (cluster dev deployment) because real-data validation must not touch the production install.
