// FungiForge — Nextflow configuration
// Two composable profile groups (mirrors CallForge / MethylForge):
//   execution : local | hpc_slurm
//   packaging : conda | mamba | docker | singularity | apptainer
// e.g.  -profile local,docker            (this Apple-Silicon Mac; linux/amd64 emulated)
//       -profile hpc_slurm,singularity   (lab Linux HPC; native)

manifest {
  name            = 'fungiforge'
  author          = 'aduton1000'
  description     = 'Reproducible fungal genomics from ONT (+optional Illumina hybrid): assembly, eukaryotic annotation, ITS/genome identification, antifungal-resistance calling, mobile/TE/mycovirus, biosynthetic gene clusters, novelty, and One Health comparative analysis.'
  version         = '0.1.0'
  nextflowVersion = '>=23.10'
}

params {
  help          = false
  // ---- inputs ----
  samplesheet   = null   // CSV: sample,ont_fastq,illumina_r1,illumina_r2,compartment,facility,season
  outdir        = 'results'
  data_dir      = null   // reference-database root (see bin/fetch_references.sh); external SSD on Mac

  // ---- read handling ----
  ont_min_qual  = 10     // chopper Q filter
  ont_min_len   = 1000   // chopper length filter
  hybrid        = 'auto' // auto (use Illumina if present) | on | off  -> Stage 03 polishing
  basecall      = false  // true: run Dorado on pod5 input (ont_fastq column = pod5 dir)
  dorado_model  = 'r1041_e82_400bps_sup_v5.2.0'
  dorado_duplex = false

  // ---- assembly / QC ----
  assembler     = 'flye' // flye | canu | raven
  purge_dups    = true
  busco_lineage = 'auto' // auto (order-specific after ID) | fungi_odb10 | <lineage>

  // ---- annotation ----
  genemark_key  = null   // path to a (free academic) GeneMark license key .gm_key
  run_interproscan = false  // heavy/slow under emulation — default off on Mac, on for HPC
  ploidy        = 'auto'

  // ---- resistance ----
  af_panel      = "${projectDir}/fungiforge/resources/af_resistance_panel.tsv"

  // ---- stage toggles (skip anything not wanted) ----
  skip_decontam    = false
  skip_mge         = false
  skip_bgc         = false
  skip_novelty     = false
  skip_extras      = false

  // ---- compute caps ----
  max_cpus      = 16
  max_memory    = '120.GB'
  max_time      = '96.h'   // fungal annotation is slow, esp. emulated
}

process {
  cpus   = { Math.min(4  * task.attempt, params.max_cpus as int) }
  memory = { (8.GB * task.attempt) }
  time   = { (4.h  * task.attempt) }
  errorStrategy = { task.exitStatus in [137,139,140,143,247] ? 'retry' : 'finish' }
  maxRetries = 2
}

profiles {
  // ---- execution ----
  local     { process.executor = 'local' }
  hpc_slurm { includeConfig 'conf/hpc_slurm.config' }
  // ---- packaging ----
  conda      { conda.enabled = true;  conda.useMamba = false }
  mamba      { conda.enabled = true;  conda.useMamba = true  }
  // -e HOME/USER so tools that call getpass.getuser()/expanduser (medaka/torch, funannotate)
  // don't crash on a host UID that isn't in the container's /etc/passwd.
  docker     { docker.enabled = true; docker.runOptions = '--platform linux/amd64 -u $(id -u):$(id -g) -e HOME=/tmp -e USER=fungiforge' }
  singularity{ singularity.enabled = true; singularity.autoMounts = true }
  apptainer  { apptainer.enabled  = true; apptainer.autoMounts  = true }
  // ---- test ----
  test       { includeConfig 'conf/test.config' }
}

// base.config (labels -> env/container + resources) loaded AFTER profiles so its
// withLabel resource values are the baseline; hpc_slurm overrides executor only.
includeConfig 'conf/base.config'

report   { enabled = true; file = "${params.outdir}/pipeline_info/execution_report.html"; overwrite = true }
timeline { enabled = true; file = "${params.outdir}/pipeline_info/timeline.html"; overwrite = true }
trace    { enabled = true; file = "${params.outdir}/pipeline_info/trace.txt"; overwrite = true }
